## 

 Tier 0 `dispatch::call_value_inner` (dispatch.rs:1197)  `dispatch::call_task_inner` (dispatch.rs:1137)  `self.evaluate`/`self.execute`  AST 

AGENTS_CODE_MODIFICATION §2`Value::Closure`  `Value::Task`  AST  AST executor =  Value  MirFunction bodyMIR  body  `run_mir` fallback  arena  struct  / tool  / trait default impl

## 6  80  + 12 

### 1. `src/value.rs` —  `Value::Closure`  `Value::Task`

```rust
Closure {
    params: Vec<String>,
    env: EnvRef,
    v2_node_id: Option<usize>,
    /// α.x: MIR-built closure body. Some → run_mir dispatch. None → arena fallback.
    mir_body: Option<std::sync::Arc<crate::mir::MirFunction>>,
},
Task {
    name: String,
    params: Vec<String>,
    v2_body_ids: Vec<usize>,
    /// α.x: MIR-built task body (for α.7/α.8 trait/impl/skill). Same dispatch rule.
    mir_body: Option<std::sync::Arc<crate::mir::MirFunction>>,
},
```

-  `Arc<MirFunction>` Rc `Value: Send + Sync`http_server `Arc<RwLock<HashMap<...,Value>>>` 
- Display / PartialEq  `{ .. }` pattern

### 2. `src/mir/mod.rs` —  `MirInst::Closure`

 TaskDef lower.rs:328-344
```rust
Closure { params: Vec<String>, body: Box<MirFunction> },
```

### 3. `src/mir/lower.rs` —  `ExprKind::Closure` 

 `TaskDef` loweringline 327-343 lower body  `MirFunction`emit `MirInst::Closure``return_type` typeck 

### 4. `src/mir/interp.rs` —  `MirInst::Closure` handler

```rust
MirInst::Closure { params, body } => {
    let closure = Value::Closure {
        params: params.clone(),
        env: EnvRef::from_arc_mutex(interp.core.environment.clone()),
        v2_node_id: None,
        mir_body: Some(Arc::new((*body).clone())),
    };
    regs[*dst] = closure;
    pc += 1;
}
```

`StructDef` handler (line 152-164)  `mir_body: None`

### 5. `src/interpreter/dispatch.rs` — `call_value`  `call_value_inner` MIR 

```rust
// call_value (line 1096)  MIR 
Value::Closure { mir_body: Some(body), .. } => {
    //  run_mir arena
    let mut child_env = env_from_ref(...);
    body.params.iter().zip(args).for_each(...);
    run_mir(body, interp, &mut child_env)
}
// Value::Task { mir_body: Some(body), .. } 
```

`call_value_inner`  line 1197  MIR  arena 

### 6. `src/interpreter/execute.rs` + `src/mir/interp.rs` + `src/mock/mod.rs` —  `mir_body: None`

12  positional struct initexecute.rs:341,762,795,915,928; mir/interp.rs:491,525,601,613; mock/mod.rs:142; value.rs: `mir_body: None`AST struct  / tool  / trait default impl  arena body Value::Closure  MIR loweringMIR  Closure  arena

## 

- `Value::Closure { v2_node_id }` / `Value::Task { v2_body_ids }` ——struct v2_node_id=Nonetool trait default implskill verify`execute_trait_def` / `execute_impl_def` / `execute_skill_def` AST 
- `evaluate.rs` / `execute.rs` / `evaluate_closure` ——differential test `tests/mir_differential.rs` 
- `dispatch::call_function` builtin —— `tests/tier0_builtin_dispatch.rs` 

## 

- `src/value.rs`, `src/mir/mod.rs`, `src/mir/lower.rs`, `src/mir/interp.rs`, `src/interpreter/dispatch.rs`, `src/interpreter/execute.rs`, `src/mock/mod.rs`
- `src/interpreter/evaluate.rs`, `src/interpreter/orchestrate.rs`, `src/interpreter/orchestrate_v2.rs`, `src/interpreter/trait_dispatch.rs`, `src/http_server.rs`, `src/mcp_server.rs`, `src/main.rs`
- `Value::Closure` / `Value::Task`  `mir_body` 

## AGENTS_CODE_MODIFICATION §6

1. `cargo build --all-targets` —  0 
2. `cargo test --all` —  838+ 
3. `cargo clippy --all-targets -- -D warnings` — 0 
4. `cargo fmt --check` — 0 diff
5.  `tests/tier0_closure_mir.rs` 
   - `closure_literal_then_call``let f = fn(x) x*2 end; f(5)`  MIR 
   - `closure_captures_env` MIR 
   - `closure_return_propagation` `return`  MIR 
6.  `tests/tier0_builtin_dispatch.rs` closure/task MIR  arena dispatch

## 

- `call_value_inner` / `call_task_inner`  `self.evaluate` / `self.execute`  5  1  `Value::Closure { mir_body: None, v2_node_id: Some(_) }`  legacy 
- / `run_mir`AST 
- 838  +  closure 
- Differential test  AST 