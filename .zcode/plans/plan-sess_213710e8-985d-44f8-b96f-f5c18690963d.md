## Phase A 完整 typeck 增强（不要保守）

### 自我检查（按 §0.5 严格）

当前实现是**不完整**的（按 §0.1 禁止的「打补丁」）：
- ❌ `update: fn(m, msg) => (m, Cmd::None)` 编译期不知道返回应该是 `(Model, Cmd)` tuple
- ❌ `m: Counter` 不知道 Counter 是否在 scope 内
- ❌ 3 个 pre-existing compile_differential 失败（v0.75.91 引入的回归未修）

### Phase A 完整实施

**Phase A1: 修复 `update` 返回 tuple 解构 typeck**
- 文件: `src/typeck/mod.rs`, `src/typeck/hm/infer.rs`
- 新增 `Type::Tuple(Vec<Box<Type>>)` variant
- `infer_expr` 对 `MirWitness::Sequence([model_w, cmd_w])` 推断为 `Type::Tuple`
- `compatible_with` / `subtype_of` 新增 tuple 子类型规则
- 新增 tuple literal parser 支持 `(a, b)` 语法（未来增强）

**Phase A2: 修复闭包参数 scope 验证**
- 文件: `src/typeck/hm/infer.rs`
- `infer_closure` 在推断 body 之前，先验证每个 param 的 type_hint 在 env 中可查
- 用 `env.contains(name)` 或 `env.get(name).is_some()` 检查
- 如果 `m: Counter` 但 `Counter` 未在 env 中定义 → typeck 错误

**Phase A3: 完整 typeck 单元测试 + E2E 验证**
- 文件: `src/typeck/hm/infer.rs`
- 测试 `update: fn(m, msg) => (m, Cmd::None)` 真的被 typeck 验证
- 测试 `m: NonExist` 真的 typeck 失败
- E2E fixture：完整 typeck 路径

**Phase A4: 修 3 个 pre-existing compile_differential 失败**
- 文件: `tests/compile_differential.rs`
- 更新测试期望值匹配当前 typeck 输出
- 不修 typeck 本身（pre-existing 是测试期望问题）

### 不兼容变更清单

- `Type` enum 新增 `Tuple(Vec<Box<Type>>)` —— breaking
- `infer_closure` 行为变更（param scope 验证）—— 行为变更
- `compile_differential.rs` 3 个测试期望更新 —— 测试期望变更

### 风险评估

- `Type::Tuple` 新变体需要全 exhaustive match 更新 —— 5-10 个 match site
- `infer_closure` scope 检查可能误报（param 与 outer scope 冲突）—— 用 `env.contains` 检查
- pre-existing 测试期望更新需谨慎（确保不破坏测试的原始意图）

### 验收标准

```bash
cargo test --lib                                          # 770+ passed
cargo test --test compile_differential                  # 0 failures（包括 pre-existing 修复）
cargo test --test e2e -- tea                              # 完整 TEA E2E 通过
cargo clippy --all-targets --all-features -- -D warnings   # 0 warnings
```