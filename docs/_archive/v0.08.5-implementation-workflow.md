# Mora v0.09 

commit `766170d v0.08: trait ` 
 v0.09 commit
2026-06-20

> ****v0.08 trait  6  v0.09 

## 0. 

|  | commit |  |
|------|--------|----------|
| v0.08 | `766170d` | trait  + dyn dispatch +  +  |
| v0.09 |  commit | trait 5 finding + 1  + 1 CLI fix |

## 1. 

| # |  | Finding |  |
|---|--------|---------|---------|
| F1 |  |  trait instance  trait  |  collect_required_methods()  BFS |
| F2 |  | `_type`/`_trait`  `Value::Dict`  trait-object  | `Value::TraitObject`  `for_type`/`trait_name`/`data`  |
| F3 |  | `Type::Struct(name, Vec<String>)` typeck  interpreter  |  enum variant `Type::Trait(trait_name)` |
| F4 |  |  trait  span | `dispatch_trait_method` + `construct_trait_instance`  `call_site: Span` |
| F5 |  | trait  0  | interpreter.rs  5  + typeck_errors.mora  2  case |
| F6 | 🟡 | `__impl_<Trait>_<ForType>_<method>`  6  |  `impl_method_key()` / `default_impl_method_key()`  |
|  | 🟢 | 6  minor  docv0.09  | typeck.rs / interpreter.rs /  |
| CLI | 🟢 | `mora run <file>`  bug | main.rs  `run`  |

## 2. 

### 2.1 `Value::TraitObject` 

```rust
// v0.08 ()
TraitObject {
    data: Box<Value>,
    vtable: HashMap<String, HashMap<String, Value>>,
}

// v0.09 ()
TraitObject {
    for_type: String,
    trait_name: String,
    data: Box<Value>,
}
```

`vtable` —— vtable  closure dispatch  `dispatch_trait_method()`  `__impl_*`  env vtable closure`vtable` 

### 2.2 

```rust
pub(crate) fn impl_method_key(trait_name: &str, for_type: &str, method: &str) -> String {
    format!("__impl_{}_{}_{}", trait_name, for_type, method)
}

pub(crate) fn default_impl_method_key(trait_name: &str, method: &str) -> String {
    format!("__impl_{}_{}_{}", trait_name, trait_name, method)
}

pub(crate) fn collect_required_methods<'a>(
    trait_registry: &'a HashMap<String, TraitInfo>,
    trait_name: &str,
) -> Vec<&'a str> { /* BFS  */ }
```

`collect_required_methods`  `construct_trait_instance` LSP  typeck  `collect_trait_methods_recursive` 

## 3. 

|  | v0.08.4 | v0.09 |
|------|---------|------|
|  | 47 | 52 (+5) |
|  |  |  |
| examples  | 9/9 | 9/9 |
| typeck e2e  | 23 | 25 (+2) |
| trait  span |  |  |

## 4. v0.09 → v0.10 

| v0.10  | v0.09  |
|------------|------------------|
| trait  `trait Foo<T>` | TraitMethod / TraitInfo / TraitTypeDef  `generic_params: Vec<String>`  |
| impl  `impl Foo<T> for Bar` | `Type::Struct` Type::Trait  |
| `__impl_*`  | `impl_method_key()`  1  |
|  dispatch chain | trait registry + dispatch_trait_method  +  |
| `mimo web`  | MiMo Code GitHub  3 index.ts × 2 + web.ts × 1rebuild mimo.exe  npm  |

## 5. v0.09 

|  |  |
|------|------|
|  self-less  | trait method  `self`  |
|  | `= expr` `do ... end`  |
|  trait  | trait  |
|  where  | `impl Foo for Type where Type: Bar`  |
| Monomorphize |  dispatch  dyn |
| CLI  bug | `mimo run <file>`  bug v0.09  |

## 6. 

- `src/interpreter.rs`: +481/-169TraitObject  + 4  helper + 5  + span 
- `src/typeck.rs`: +9/-11 `Type::Struct` `Type::Trait`
- `src/main.rs`: +8CLI bug fix
- `README.md`: +31trait 
- `docs/v0.08.5-implementation-workflow.md`: +183
- `test_data/typeck_errors.mora`: +162  trait  case
- `.gitignore`: +2sdd/ ignore

---

****2026-06-20
****Mora 
**License**BSD-3-Clause
