# Mora v0.09 trait 

> ****v0.08.5 v0.09 = trait  + impl  + where  + 
> ****—— v0.08 trait 

## 0. 

v0.08 trait commit `01c3015`

- trait `trait Name [: Parent, ...]`
- `fn name(params) = expr` / `do ... end`
- `trait Foo: Bar, Baz`
- self-having / self-less 
- dyn dispatch`Trait::new("ForType")`

**v0.08 ** 4  5-8 

1. trait 
2. impl 
3.  where 
4.  dyn dispatch + 

## 1. 

v0.09  **3  + 1 **

|  |  |  |  |
|------|------|--------|--------|
| 3 | trait  `trait Foo<T>` |   | 1-2  |
| 4 | impl  `impl Foo<T> for Bar` |   | 1  |
| 5 | where  | 🟡  | 1  |
| 6 |  | 🟢  | 2-3  |

v0.09 =  3+4+5trait/impl  + where 6 v0.10

## 2. 

### 2.1  trait 

```mora
-- trait  T
trait Container<T>
  fn get(self) -> T
  fn set(self, value: T)
end

-- trait  + boundwhere 
trait Map<K, V>
  fn put(self, key: K, value: V)
  fn get(self, key: K) -> V
end
```

### 2.2  impl

```mora
-- impl  T  trait
impl Container<number> for BoxedNum
  fn get(self) = self.value
  fn set(self, value) = self.value = value
end

-- impl  trait
impl Container<T> for Boxed<T>
  fn get(self) = self.value
  fn set(self, value) = self.value = value
end
```

### 2.3 where 

```mora
-- where 
fn sort<T>(list: list<T>) -> list<T> where T: Comparable
  -- ...
end

-- trait impl  where
impl Container<T> for Wrapper<T> where T: Clone
  fn get(self) = self.value.clone()
  fn set(self, value) = self.value = value
end
```

### 2.4 

```mora
-- 
let c: dyn Container<number> = Container::new("BoxedNum")
let v = c.get()  -- v  number

-- v0.09  / v0.10 
let c = Container<number>::new("BoxedNum")  -- 
```

## 3. AST 

### 3.1  `GenericParam` 

```rust
// src/ast.rs 
pub struct GenericParam {
    pub name: String,           // "T"
    pub bound: Option<String>,  // v0.09  stringv0.10  Vec<TraitRef>
    pub span: Span,
}
```

### 3.2  `Stmt::TraitDef`

```rust
TraitDef {
    name: String,
    /// v0.09 trait 
    generics: Vec<GenericParam>,
    parents: Vec<String>,
    methods: Vec<TraitMethod>,
    span: Span,
}
```

### 3.3  `Stmt::ImplDef`

```rust
ImplDef {
    /// v0.09 impl  `impl<T> Foo<T> for Bar`
    generics: Vec<GenericParam>,
    /// v0.09 trait  `Foo<T>`  `<T>`
    trait_generics: Vec<String>,
    trait_name: String,
    for_type: String,
    /// v0.09 for_type  `Bar<U>`  `<U>`
    for_generics: Vec<String>,
    /// v0.09 where 
    where_clause: Vec<GenericParam>,
    methods: Vec<FnDef>,
    span: Span,
}
```

### 3.4  `TraitMethod`

```rust
pub struct TraitMethod {
    pub name: String,
    pub params: Vec<(String, Option<String>)>,
    pub return_type: Option<String>,
    pub body: Vec<Stmt>,
    /// v0.09 trait method  Rust trait 
    pub generics: Vec<GenericParam>,
    pub span: Span,
}
```

### 3.5  `Expr::DynTrait` 

```rust
DynTrait {
    /// v0.09  `dyn Container<number>`  `<number>`
    generics: Vec<String>,
    trait_name: String,
    span: Span,
}
```

## 4. Type 

### 4.1 `Type::Trait` 

```rust
pub enum Type {
    // v0.08: Trait(String)
    // v0.09 
    Trait {
        name: String,
        generics: Vec<Type>,
    },
    //  Type::Structv0.08.5 
    // ...
}
```

### 4.2 `Type::Struct` `Type::Concrete`

```rust
pub enum Type {
    //  v0.08.5  Struct trait  + 
    Concrete {
        name: String,
        generics: Vec<Type>,
        traits: Vec<Type>,  //  trait
    },
    // ...
}
```

### 4.3 Trait 

`impl_method_key` / `default_impl_method_key` 

```rust
pub(crate) fn impl_method_key(
    trait_name: &str,
    trait_generics: &[Type],
    for_type: &str,
    for_generics: &[Type],
    method: &str,
) -> String {
    // : __impl_<Trait>_<TraitGen>_<ForType>_<ForGen>_<method>
    // TraitGen: "T=number,U=string"
    // :  Type `String` / `Number`
    let tg = trait_generics.iter().map(type_name).collect::<Vec<_>>().join(",");
    let fg = for_generics.iter().map(type_name).collect::<Vec<_>>().join(",");
    format!("__impl_{}_{}_{}_{}_{}", trait_name, tg, for_type, fg, method)
}
```

****
- `Container<number>` for `BoxedNum` → `__impl_Container_number_BoxedNum__get`
- `Map<string, number>` for `DictMap` → `__impl_Map_string,number_DictMap__get`

## 5. Parser 

### 5.1 trait 

```rust
fn parse_trait_def(&mut self) -> Stmt {
    // ... 
    let generics = if self.match_token(&[TokenType::LBracket]) {  // <T>
        //  <T>  <T: Bound>  <T, U> 
        self.parse_generic_params()
    } else { vec![] };
    // ...
}

fn parse_generic_params(&mut self) -> Vec<GenericParam> {
    //  <T>  <T: Bound>  <T, U, V: Bound>
}
```

### 5.2 impl 

```mora
impl<T> Container<T> for Boxed<T> where T: Clone
  -- body
end
```

```rust
fn parse_impl_def(&mut self) -> Stmt {
    let generics = if self.match_token(&[TokenType::LBracket]) {
        self.parse_generic_params()
    } else { vec![] };
    let trait_name = self.consume_identifier(...);
    let trait_generics = if self.match_token(&[TokenType::LBracket]) {
        self.parse_type_list()
    } else { vec![] };
    self.consume(For, ...);
    let for_type = self.consume_identifier(...);
    let for_generics = if self.match_token(&[TokenType::LBracket]) {
        self.parse_type_list()
    } else { vec![] };
    let where_clause = if self.match_token(&[TokenType::Where]) {
        self.parse_where_clause()
    } else { vec![] };
    // ...
}
```

### 5.3 where 

```rust
fn parse_where_clause(&mut self) -> Vec<GenericParam> {
    //  `T: Bound, U: Bound2, ...`
    // bound  trait "Comparable"
}
```

## 6. Typeck 

### 6.1 TraitTypeDef 

```rust
struct TraitTypeDef {
    name: String,
    parents: Vec<String>,
    /// v0.09: trait 
    generics: Vec<String>,  // ["T", "U"]
    /// v0.09:  has_self + 
    methods: Vec<(String, Signature, bool, bool, Vec<String>)>,
    //                ↑name   ↑sig   ↑def ↑self ↑generics
}
```

### 6.2 

```rust
pub struct TypeChecker {
    // ... 
    /// v0.09:  trait dispatch 
    /// `Container<number>`  T=number
    type_bindings: HashMap<String, Type>,
}
```

### 6.3 

```rust
// impl Container<T> for Bar
//   trait Container  1  generic
//   for_type Bar  0  generic <...>
// typeck : "expected 1 type arg for trait 'Container', got 0"
```

### 6.4 where 

```rust
// impl<T> Container<T> for Wrapper<T> where T: Clone
//    typeck  T  Clone trait
//   v0.09  where  trait  trait_registry 
//   v0.10  where bound
```

## 7. Interpreter 

### 7.1  trait 

`execute(TraitDef)`  `TraitInfo`  `generics: Vec<String>`

```rust
self.trait_registry.insert(name.clone(), TraitInfo {
    name: name.clone(),
    generics: trait_def.generics.iter().map(|g| g.name.clone()).collect(),
    parents: parents.clone(),
    methods: method_sigs,
});
```

### 7.2  impl 

`execute(ImplDef)`  key

```rust
let key = impl_method_key(
    &trait_name, &trait_generics,    // "Container", ["number"]
    &for_type, &for_generics,        // "BoxedNum", []
    &m.name,
);
self.environment.lock().unwrap().define(key, task);
```

### 7.3 dispatch  trait

`dispatch_trait_method`  TraitObject  `trait_name + generics` 

```rust
// TraitObject  generics
TraitObject {
    for_type: String,
    for_generics: Vec<Type>,  // v0.09 
    trait_name: String,
    trait_generics: Vec<Type>,  // v0.09 
    data: Box<Value>,
}
```

## 8. 

### 8.1 Container trait

```mora
-- container.mora
trait Container<T>
  fn get(self) -> T
  fn set(self, value: T)
end

impl Container<number> for BoxedNum
  fn get(self) = self.value
  fn set(self, value) = self.value = value
end

task main()
  let c: dyn Container<number> = Container<number>::new("BoxedNum")
  c.set(42)
  print(c.get())  --  42
end
```

### 8.2  + where

```mora
-- generic_with_where.mora
trait Comparable
  fn less(self, other: Self) -> bool
end

trait Container<T> where T: Comparable
  fn min(self, other: T) -> T
end

impl Container<number> for NumPair where number: Comparable
  fn min(self, other) = if self.less(other) then self else other
end

impl Comparable for number
  fn less(self, other) = self < other
end

task main()
  let p: dyn Container<number> = Container<number>::new("NumPair")
  print(p.min(5))  -- 5
end
```

### 8.3 

```mora
-- nested.mora
trait Boxed<T>
  fn unbox(self) -> T
end

trait Container<T>
  fn get(self) -> T
end

-- Boxed<Boxed<number>> —— 
impl Container<Boxed<number>> for Outer
  fn get(self) = self.inner
end

task main()
  let c: dyn Container<Boxed<number>> = Container<Boxed<number>>::new("Outer")
  let inner = c.get()
  print(inner.unbox())  --  box  number
end
```

## 9. v0.09  v0.10+

-  ****——  dyn dispatch
-  ****`let x = container.get()`  x —— 
-  ** trait bound (HRTB)** —— v0.09 
-  ****`type Item`—— v0.10+
-  **trait object safety **v0.09  trait  dyn
-  ** task**`task foo<T>(x: T)`——  trait  v0.11

## 10. 

-  **v0.08  examples ** trait 
-  **commit `01c3015` ** trait dispatch  v0.08 
-  ** 57 ** trait 
-  **19/19 e2e **

## 11. 

| Step |  |  |  |
|------|------|---------|--------|
| 1 | AST  | `src/ast.rs` | 🟢  |
| 2 | Parser trait/impl  + where | `src/parser.rs` | 🟡  |
| 3 | Type Trait/Concrete  | `src/typeck.rs` | 🟡  |
| 4 | Typeck  | `src/typeck.rs` | 🟡  |
| 5 | Interpreter TraitInfo  | `src/interpreter.rs` | 🟡  |
| 6 | dispatch key  | `src/interpreter.rs` | 🟡  |
| 7 | TraitObject  | `src/interpreter.rs` | 🟡  |
| 8 | impl_method_key  | `src/interpreter.rs` | 🟢  |
| 9 | 5-8  | `src/interpreter.rs` | 🟢  |
| 10 | e2e case2-3  | `test_data/typeck_errors.mora` | 🟢  |
| 11 | examples2-3  | `examples/` | 🟢  |
| 12 |  | `docs/v0.09-implementation-workflow.md` | 🟢  |

****5-7 

## 12. 

|  |  |  |  |
|------|------|------|------|
| `Type::Struct` v0.08  typeck  | 🟡  |   |  `Type::Concrete`  |
|  trait  key  key | 🟢  | 🟡  | key  generics  |
| v0.09 "" |   | 🟡  | —— v0.10 |
| where v0.09  | 🟡  | 🟡  | v0.09  where  trait v0.10  |
|  trait examples  | 🟢  |   | v0.08 examples  |

## 13. 

-  `trait Container<T>`  impl dispatch
-  `impl Container<number> for BoxedNum` 
-  `impl<T> Container<T> for Boxed<T>`  impl 
-  `where T: Comparable` typeck 
-   57  + 9 trait examples + 19/19 e2e 
-   3  examples container / generic_with_where / nested
-  `docs/v0.09-implementation-workflow.md` 

## 14.  MiMo Code 

- v0.08  MiMo Code commit `01c3015`
- v0.09 trait MiMo Code LSP 
- LSP hover/completion  trait v0.10 

---

****Mora 
****2026-06-20
**License**BSD-3-Clause
****`docs/v0.08-trait-system-workflow.md``docs/v0.08.5-implementation-workflow.md`
