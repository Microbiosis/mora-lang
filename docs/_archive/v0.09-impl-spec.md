# Mora v0.09 AST / Type / Parser 

> ****`docs/v0.09-design.md` v0.09 "code-level"
> **** v0.09 
> ****12-18 1.5-2 

## 1. 

 v0.09  **AST / Type / Parser ** typeck  / interpreter dispatch /  / examples v0.09-design.md 

## 2. AST src/ast.rs

### 2.1  `GenericParam` 

```rust
/// v0.09: trait/impl/method 
///    `trait Foo<T>` / `impl<T> Foo<T> for Bar`  `T`
///   v0.09 : bound  trait  "Comparable"v0.10  Vec<TraitRef>
#[derive(Debug, Clone, PartialEq)]
pub struct GenericParam {
    pub name: String,
    /// v0.09: bound trait v0.10 
    pub bound: Option<String>,
    pub span: Span,
}
```

### 2.2  `Stmt::TraitDef`

```rust
TraitDef {
    name: String,
    /// v0.09 : trait  `trait Container<T>`  `<T>`
    pub generics: Vec<GenericParam>,
    parents: Vec<String>,
    methods: Vec<TraitMethod>,
    span: Span,
},
```

### 2.3  `Stmt::ImplDef`

```rust
ImplDef {
    /// v0.09 : impl  `impl<T> Container<T> for Bar<T>`  `<T>`
    pub generics: Vec<GenericParam>,
    /// v0.09 : trait  `Container<T>`  `<T>`
    pub trait_generics: Vec<String>,
    trait_name: String,
    for_type: String,
    /// v0.09 : for_type  `Bar<U>`  `<U>`
    pub for_generics: Vec<String>,
    /// v0.09 : where  `where T: Comparable`
    pub where_clause: Vec<GenericParam>,
    methods: Vec<FnDef>,
    span: Span,
},
```

### 2.4  `TraitMethod`trait method 

```rust
pub struct TraitMethod {
    pub name: String,
    pub params: Vec<(String, Option<String>)>,
    pub return_type: Option<String>,
    pub body: Vec<Stmt>,
    /// v0.09 : trait method  Rust trait 
    pub generics: Vec<GenericParam>,
    pub span: Span,
}
```

### 2.5  `Expr::DynTrait`

```rust
DynTrait {
    /// v0.09 :  `dyn Container<number>`  `<number>`
    pub generics: Vec<String>,
    trait_name: String,
    span: Span,
},
```

## 3. Type src/typeck.rs

### 3.1 `Type::Trait`  struct variant

```rust
pub enum Type {
    // ... 
    
    /// v0.09:  trait 
    ///  `dyn Container<number>` → Trait { name: "Container", generics: [Type::Number] }
    Trait {
        name: String,
        generics: Vec<Type>,
    },
    
    /// v0.09 :  v0.08.5  Type::Struct
    ///  +  trait 
    Concrete {
        name: String,
        generics: Vec<Type>,
        traits: Vec<Type>,  //  trait
    },
    
    // ...
}
```

### 3.2 `Type::from_hint`  `dyn:Foo<T>`

```rust
pub fn from_hint(hint: &str) -> Type {
    if let Some(rest) = hint.strip_prefix("dyn:") {
        //  `Foo`  `Foo<T,U>`
        if let Some(lt) = rest.find('<') {
            let name = rest[..lt].to_string();
            let generics_str = &rest[lt+1..rest.len()-1];  // strip < >
            let generics: Vec<Type> = generics_str
                .split(',')
                .map(|s| Type::from_hint(s.trim()))
                .collect();
            Type::Trait { name, generics }
        } else {
            Type::Trait { name: rest.to_string(), generics: vec![] }
        }
    } else {
        // ... 
    }
}
```

### 3.3 `Type::name()` 

```rust
pub fn name(&self) -> String {
    match self {
        Type::Trait { name, generics } => {
            if generics.is_empty() {
                format!("dyn:{}", name)
            } else {
                let gs: Vec<String> = generics.iter().map(|g| g.name()).collect();
                format!("dyn:{}<{}>", name, gs.join(","))
            }
        }
        Type::Concrete { name, generics, .. } => {
            if generics.is_empty() {
                name.clone()
            } else {
                let gs: Vec<String> = generics.iter().map(|g| g.name()).collect();
                format!("{}<{}>", name, gs.join(","))
            }
        }
        // ... 
    }
}
```

### 3.4 `Type::compatible_with` 

```rust
pub fn compatible_with(&self, expected: &Type) -> bool {
    if matches!(self, Type::Any) || matches!(expected, Type::Any) { return true; }
    if matches!(self, Type::Nil) { return true; }
    
    // v0.09: Trait 
    if let (Type::Trait { name: a, generics: ga }, Type::Trait { name: b, generics: gb }) = (self, expected) {
        if a != b { return false; }
        if ga.len() != gb.len() { return false; }
        for (x, y) in ga.iter().zip(gb.iter()) {
            if !x.compatible_with(y) { return false; }
        }
        return true;
    }
    
    // v0.09: Concrete  Trait 
    if let (Type::Concrete { name: c, generics: gc, traits: ct }, Type::Trait { name: tn, generics: tg }) = (self, expected) {
        for t in ct {
            if let Type::Trait { name, generics } = t {
                if name == tn && generics.len() == tg.len() {
                    // : 
                    return true;
                }
            }
        }
        return false;
    }
    
    // 
    self == expected
}
```

## 4. Parser src/parser.rs

### 4.1 

#### `parse_generic_params()`

```rust
///  `<T>`  `<T: Bound>`  `<T, U, V: Bound>`
/// : `Vec<GenericParam>`
fn parse_generic_params(&mut self) -> Result<Vec<GenericParam>, String> {
    self.consume(&TokenType::Less, "Expected '<' for generic params")?;
    let mut params = vec![self.parse_single_generic_param()?];
    while self.match_token(&[TokenType::Comma]) {
        params.push(self.parse_single_generic_param()?);
    }
    self.consume(&TokenType::Greater, "Expected '>' after generic params")?;
    Ok(params)
}

fn parse_single_generic_param(&mut self) -> Result<GenericParam, String> {
    let span = self.span_of_previous_keyword();
    let name = self.consume_identifier("Expected generic param name")?;
    let bound = if self.match_token(&[TokenType::Colon]) {
        Some(self.consume_identifier("Expected bound trait name after ':'")?)
    } else { None };
    Ok(GenericParam { name, bound, span })
}
```

#### `parse_type_list()`

```rust
///  `<T, U, V>`  trait_generics / for_generics
/// : `Vec<String>` ( string )
fn parse_type_list(&mut self) -> Result<Vec<String>, String> {
    self.consume(&TokenType::Less, "Expected '<' for type list")?;
    let mut types = vec![self.consume_identifier("Expected type name")?];
    while self.match_token(&[TokenType::Comma]) {
        types.push(self.consume_identifier("Expected type name")?);
    }
    self.consume(&TokenType::Greater, "Expected '>' after type list")?;
    Ok(types)
}
```

#### `parse_where_clause()`

```rust
///  `where T: Bound, U: Bound2`
fn parse_where_clause(&mut self) -> Result<Vec<GenericParam>, String> {
    self.consume(&TokenType::Where, "Expected 'where' keyword")?;
    let mut clauses = vec![self.parse_single_generic_param()?];
    while self.match_token(&[TokenType::Comma]) {
        clauses.push(self.parse_single_generic_param()?);
    }
    Ok(clauses)
}
```

### 4.2 `parse_trait_def`  generics

```rust
fn parse_trait_def(&mut self) -> Stmt {
    let span = self.span_of_previous_keyword();
    let name = self.consume_identifier("Expected trait name");
    
    // v0.09:  trait 
    let generics = if self.check(&TokenType::Less) {
        self.parse_generic_params().unwrap_or_else(|e| { self.error(e); vec![] })
    } else { vec![] };
    
    // :  parents
    let parents = if self.match_token(&[TokenType::Colon]) { /* ... */ } else { vec![] };
    
    // ... methods  ...
    
    Stmt::TraitDef { name, generics, parents, methods, span }
}
```

### 4.3 `parse_impl_def`  + where

```rust
fn parse_impl_def(&mut self) -> Stmt {
    let span = self.span_of_previous_keyword();
    
    // v0.09: impl  `impl<T, U>`
    let generics = if self.check(&TokenType::Less) {
        self.parse_generic_params().unwrap_or_else(|e| { self.error(e); vec![] })
    } else { vec![] };
    
    let trait_name = self.consume_identifier("Expected trait name after 'impl'");
    
    // v0.09: trait  `Foo<T>`
    let trait_generics = if self.check(&TokenType::Less) {
        self.parse_type_list().unwrap_or_else(|e| { self.error(e); vec![] })
    } else { vec![] };
    
    self.consume(&TokenType::For, "Expected 'for' after trait name in impl");
    let for_type = self.consume_identifier("Expected type name after 'for'");
    
    // v0.09: for_type  `Bar<U>`
    let for_generics = if self.check(&TokenType::Less) {
        self.parse_type_list().unwrap_or_else(|e| { self.error(e); vec![] })
    } else { vec![] };
    
    // v0.09: where 
    let where_clause = if self.match_token(&[TokenType::Where]) {
        self.parse_where_clause().unwrap_or_else(|e| { self.error(e); vec![] })
    } else { vec![] };
    
    // ... methods  ...
    
    Stmt::ImplDef {
        generics, trait_generics, trait_name, for_type,
        for_generics, where_clause, methods, span,
    }
}
```

### 4.4 `parse_dyn_trait` 

```rust
//  evaluate /  Expr::DynTrait
DynTrait {
    generics: vec![],  // v0.08: 
    trait_name,
    span,
}

// v0.09:  `dyn Container<number>`
//  generics
```

### 4.5 lexer 

`<T>`  `<`  `>`  token`Less` / `Greater`

****
- `<`  `IDENT` → 
- `<`  → 

parser  lookahead (`self.peek()`) 

## 5. 

### 5.1 AST literal 

`src/interpreter.rs` test `examples/*.mora` 

**TraitDef **
```rust
// v0.08
Stmt::TraitDef { name, parents, methods, span }

// v0.09
Stmt::TraitDef { name, generics: vec![], parents, methods, span }
```

**ImplDef **
```rust
Stmt::ImplDef {
    generics: vec![],
    trait_generics: vec![],
    trait_name,
    for_type,
    for_generics: vec![],
    where_clause: vec![],
    methods,
    span,
}
```

**DynTrait **
```rust
Expr::DynTrait { generics: vec![], trait_name, span }
```

### 5.2  `..Default::default()` 

```rust
Stmt::TraitDef {
    name,
    parents,
    methods,
    span,
    ..Default::default()  // generics  vec![]
}
```

 `Stmt` enum  `#[derive(Default)]` variant  `Default``Vec<T>`  vec`Option<T>`  None`bool`  false`String` 

## 6.  step

### Step 1: AST —— 30-60 
1. `src/ast.rs`  `pub struct GenericParam`
2. `Stmt::TraitDef`  `generics: Vec<GenericParam>`
3. `Stmt::ImplDef`  5 
4. `Expr::DynTrait`  `generics`
5. `TraitMethod`  `generics`
6.  literal grep + sed / 
7. `cargo build` 

### Step 2: Parser  —— 2-3 
1.  `parse_generic_params()` / `parse_single_generic_param()`
2.  `parse_type_list()`
3.  `parse_where_clause()`
4. `parse_trait_def`  generics 
5. `parse_impl_def`  generics + trait_generics + for_generics + where_clause
6. `parse_dyn_trait`  generics  `dyn Container<number>` 
7. `cargo build`  + 

### Step 3: Type  —— 2-3 
1. `Type::Trait`  `{name, generics}` struct variant
2.  `Type::Concrete`
3.  `Type::from_hint`  `dyn:Foo<...>`
4.  `Type::name()` 
5.  `Type::compatible_with` 
6. `cargo build` 

### Step 4: Typeck  —— 2-3 
1. `collect_signatures` TraitDef  generics
2. `collect_signatures` ImplDef  trait_generics / for_generics / where_clause
3. `TraitTypeDef.methods`  generics
4. impl 
5. where_clause bound trait 
6. `cargo build` + 

### Step 5: Interpreter dispatch key—— 3-4 
1. `impl_method_key()`  generics
2. `TraitInfo`  `generics: Vec<String>`
3. `execute(TraitDef)`  generics
4. `execute(ImplDef)`  key
5. TraitObject  2  generics for_generics / trait_generics
6. `construct_trait_instance` 
7. `eval_dyn_trait` 
8. `dispatch_trait_method`  TraitObject  generics 
9. `cargo build` + 

### Step 6:  —— 2 
1. trait  3 basic / multi-param / nested
2. impl  2 concrete / generic-for-generic
3. where 1 
4. typeck  3  / where  trait / 
5. `cargo test --lib --release` 

### Step 7: Examples —— 1 
1. `examples/container.mora` —— Container<number>
2. `examples/generic_with_where.mora` ——  + where
3. `examples/nested.mora` —— Boxed<Boxed<number>>
4.  12  examples 

### Step 8:  + e2e  —— 1 
1. `docs/v0.09-implementation-workflow.md`
2. `verify_typeck_errors.sh`  19/19
3. `cargo test --lib --release` 60+ passed
4. v0.09 commit message 

### 

**8 **12-18 1.5-2 

## 7. 

|  |  |  |  |
|------|------|------|------|
| `impl_method_key`  key  | 🟡  | 🟡  |  type name`Number` / `String` |
| TraitObject 4  Value enum  | 🟢  | 🟢  |  struct variant |
|  typeck v0.09  | 🟡  | 🟡  |  "v0.09 "v0.10  |
| `<` `>`  | 🟡  |   | parser `<`  IDENT →  |
|  57  | 🟢  |   |  `..Default::default()`  |

## 8. 

- [ ] 5  AST 
- [ ]  literal 
- [ ] parser 3 
- [ ] Type  4 
- [ ] typeck 
- [ ] interpreter dispatch key 
- [ ] 5+  trait 
- [ ] 3+  typeck  case
- [ ] 3+  examples
- [ ] 57  + 60+ 
- [ ] 19/19 e2e 
- [ ] docs/v0.09-implementation-workflow.md 
- [ ] v0.09 commit message `v0.09: trait  + impl  + where`

---

****Mora 
****2026-06-20
**License**BSD-3-Clause
****
- `docs/v0.09-design.md`
- `docs/v0.08-trait-system-workflow.md`v0.08 trait 
- `docs/v0.08.5-implementation-workflow.md`v0.08.5 trait hardening