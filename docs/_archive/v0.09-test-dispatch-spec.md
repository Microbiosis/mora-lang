# Mora v0.09 Runtime Dispatch + Testing 

> ****
> - `docs/v0.09-design.md`
> - `docs/v0.09-impl-spec.md`AST/Type/Parser 
> **** v0.09  interpreter dispatch + typeck  +  + examples

## 1. Interpreter 

### 1.1 `Value::TraitObject` 

```rust
/// v0.09: TraitObject 
///   `let c: dyn Container<number> = ...`  TraitObject
///   for_type = "BoxedNum", for_generics = []  ()
///   trait_name = "Container", trait_generics = [Type::Number]
TraitObject {
    ///  "BoxedNum"
    pub for_type: String,
    /// v0.09 : for_type  `Boxed<T>`  `T`
    pub for_generics: Vec<String>,
    /// trait  "Container"
    pub trait_name: String,
    /// v0.09 : trait  `Container<number>`  `number`
    pub trait_generics: Vec<String>,
    /// dict 
    pub data: Box<Value>,
},
```

****
- `let c: dyn Container<number> = Container<number>::new("BoxedNum")` →
  `TraitObject { for_type: "BoxedNum", for_generics: [], trait_name: "Container", trait_generics: ["number"], data: Dict({}) }`
- `let b: dyn Boxed<number> = Boxed<number>::new("Boxed")` →
  `TraitObject { for_type: "Boxed", for_generics: [], trait_name: "Boxed", trait_generics: ["number"], data: Dict({}) }`

### 1.2 `impl_method_key` 

```rust
/// v0.09:  impl method  key
/// : __impl_<Trait>_<TraitGen>_<ForType>_<ForGen>_<method>
///   TraitGen / ForGen  "Number" / "String"v0.09  typeck 
///
/// :  trait  key
///   Container<number> vs Container<string> →  key
pub(crate) fn impl_method_key(
    trait_name: &str,
    trait_generics: &[String],  // ["number"]  []
    for_type: &str,
    for_generics: &[String],   // ["T"]  []
    method: &str,
) -> String {
    let tg = trait_generics.join(",");
    let fg = for_generics.join(",");
    format!("__impl_{}_{}_{}_{}_{}", trait_name, tg, for_type, fg, method)
}

///  keyself  = trait 
pub(crate) fn default_impl_method_key(
    trait_name: &str,
    trait_generics: &[String],  // v0.09 
    method: &str,
) -> String {
    let tg = trait_generics.join(",");
    format!("__impl_{}_{}_{}", trait_name, tg, method)
}
```

****
- `impl Container<number> for BoxedNum`  `get`  →
  `__impl_Container_number_BoxedNum__get`
- `impl<T> Container<T> for Wrapper<T>`  `get`  `T=number`→
  `__impl_Container_number_Wrapper_number__get`
- `trait Container<T>`  `get` `T=number`→
  `__impl_Container_number__get`

### 1.3 `TraitInfo` 

```rust
pub struct TraitInfo {
    pub name: String,
    /// v0.09 : trait  `Container<T>`  `["T"]`
    pub generics: Vec<String>,
    pub parents: Vec<String>,
    pub methods: Vec<TraitMethodSig>,
}
```

### 1.4 `execute(Stmt::TraitDef)` 

```rust
Stmt::TraitDef { name, generics, parents, methods, span: _ } => {
    let method_sigs: Vec<TraitMethodSig> = methods.iter().map(|m| TraitMethodSig {
        name: m.name.clone(),
        params: m.params.clone(),
        return_type: m.return_type.clone(),
        has_self: m.params.first().map(|(n, _)| n == "self").unwrap_or(false),
    }).collect();
    self.trait_registry.insert(name.clone(), TraitInfo {
        name: name.clone(),
        generics: generics.iter().map(|g| g.name.clone()).collect(),  // v0.09
        parents: parents.clone(),
        methods: method_sigs,
    });
    //  __impl_<Trait>_<TraitGen>_<method>self  = trait  + 
    for m in methods {
        if !m.body.is_empty() {
            // v0.09: trait_generics  TraitDef 
            let tg: Vec<String> = generics.iter().map(|g| g.name.clone()).collect();
            let default_method_name = default_impl_method_key(name, &tg, &m.name);
            let td = Stmt::TaskDef {
                name: default_method_name.clone(),
                params: m.params.clone(),
                return_type: m.return_type.clone(),
                body: m.body.clone(),
                exported: false,
                span: m.span,
            };
            self.execute(&td)?;
        }
    }
    Ok(FlowSignal::None)
}
```

### 1.5 `execute(Stmt::ImplDef)` 

```rust
Stmt::ImplDef {
    generics, trait_generics, trait_name, for_type,
    for_generics, where_clause, methods, span: _,
} => {
    for m in methods {
        // v0.09:  key
        let impl_method_name = impl_method_key(
            trait_name, &trait_generics,
            for_type, &for_generics,
            &m.name,
        );
        let td = Stmt::TaskDef {
            name: impl_method_name.clone(),
            params: m.params.clone(),
            return_type: m.return_type.clone(),
            body: m.body.clone(),
            exported: false,
            span: m.span,
        };
        self.execute(&td)?;
        //  impl_tabletypeck 
        self.impl_table.entry(for_type.clone())
            .or_default()
            .push((trait_name.clone(), vec![td]));
    }
    Ok(FlowSignal::None)
}
```

### 1.6 `eval_dyn_trait` 

```rust
/// v0.09:  `dyn Container<number>` 
///   : `dyn Container<number>` 
///   : `let c: dyn Container<number> = ...` 
fn eval_dyn_trait(
    &mut self,
    trait_name: &str,
    trait_generics: &[String],  // v0.09 
) -> Result<Value, String> {
    Ok(Value::TraitObject {
        for_type: "nil".to_string(),
        for_generics: vec![],  // v0.09
        trait_name: trait_name.to_string(),
        trait_generics: trait_generics.to_vec(),  // v0.09
        data: Box::new(Value::Dict(HashMap::new())),
    })
}
```

### 1.7 `construct_trait_instance` 

```rust
/// v0.09:  trait instanceTrait::new("ForType")  Trait<...>::new("ForType")
fn construct_trait_instance(
    &mut self,
    trait_name: &str,
    trait_generics: &[String],  // v0.09 
    for_type: &str,
    for_generics: &[String],   // v0.09 
    call_site: Span,
) -> Result<Value, String> {
    // v0.09:  collect_required_methods  trait 
    let method_names = collect_required_methods(&self.trait_registry, trait_name);
    let env = self.environment.lock().unwrap();
    for m in method_names {
        // v0.09: key 
        let impl_name = impl_method_key(trait_name, trait_generics, for_type, for_generics, m);
        let default_name = default_impl_method_key(trait_name, trait_generics, m);
        let has_specific = env.get(&impl_name).is_some();
        let has_default = env.get(&default_name).is_some();
        if !has_specific && !has_default {
            drop(env);
            return Err(format!(
                "trait {} method '{}' has no impl for type '{}' and no default (line {})",
                trait_name, m, for_type, call_site.line
            ));
        }
    }
    drop(env);
    Ok(Value::TraitObject {
        for_type: for_type.to_string(),
        for_generics: for_generics.to_vec(),  // v0.09
        trait_name: trait_name.to_string(),
        trait_generics: trait_generics.to_vec(),  // v0.09
        data: Box::new(Value::Dict(HashMap::new())),
    })
}
```

### 1.8 `dispatch_trait_method` 

```rust
/// v0.09:  TraitObject  4 
fn dispatch_trait_method(
    &mut self,
    receiver: &Value,
    method: &str,
    args: Vec<Value>,
    call_site: Span,
) -> Result<Value, String> {
    // v0.09:  TraitObject  4 
    let (for_type, for_generics, trait_name, trait_generics) = match receiver {
        Value::TraitObject {
            for_type, for_generics, trait_name, trait_generics, ..
        } => (
            for_type.clone(),
            for_generics.clone(),
            trait_name.clone(),
            trait_generics.clone(),
        ),
        Value::Nil => return Ok(Value::Nil),
        _ => return Err(format!(
            "trait dispatch at line {}: receiver must be trait object or nil, got {:?}",
            call_site.line, receiver
        )),
    };
    // v0.09:  parents chain BFS  trait
    let search_chain = collect_parent_traits(&self.trait_registry, &trait_name);
    for tname in &search_chain {
        let tname_str: &str = tname;
        // v0.09:  trait_registry  method  self-having
        let has_self = self.trait_registry.get(tname_str)
            .and_then(|info| info.methods.iter().find(|m| m.name == method))
            .map(|m| m.has_self)
            .unwrap_or(true);
        // v0.09:  impl
        let impl_name = impl_method_key(tname_str, &trait_generics, &for_type, &for_generics, method);
        let env = self.environment.lock().unwrap();
        if let Some(task) = env.get(&impl_name) {
            drop(env);
            let mut all_args = if has_self { vec![receiver.clone()] } else { Vec::new() };
            all_args.extend(args);
            return self.call_value(&task, all_args);
        }
        drop(env);
        // v0.09: fallback 
        let default_name = default_impl_method_key(tname_str, &trait_generics, method);
        let env = self.environment.lock().unwrap();
        if let Some(task) = env.get(&default_name) {
            drop(env);
            let mut all_args = if has_self { vec![receiver.clone()] } else { Vec::new() };
            all_args.extend(args);
            return self.call_value(&task, all_args);
        }
        drop(env);
    }
    Err(format!(
        "trait dispatch at line {}: no impl for type '{}' method '{}' (searched: {}, generics: trait<{}> for {})",
        call_site.line, for_type, method,
        search_chain.iter().map(|s| s.to_string()).collect::<Vec<_>>().join(" → "),
        trait_generics.join(","), for_generics.join(","),
    ))
}
```

### 1.9 `call_function`  `Trait::new` 

```rust
// v0.09:  `Trait<...>::new("ForType")` 
fn call_function(&mut self, name: &str, args: Vec<Value>, call_site: Span) -> Result<Value, String> {
    //  name  "Container<number>::new"
    if name.ends_with("::new") {
        let qualified = &name[..name.len() - 5];  //  "::new"
        if let Some(lt) = qualified.find('<') {
            //  "Container<number>" → trait_name="Container", trait_generics=["number"]
            let trait_name = qualified[..lt].to_string();
            let generics_str = &qualified[lt+1..qualified.len()-1];
            let trait_generics: Vec<String> = generics_str
                .split(',').map(|s| s.trim().to_string()).collect();
            
            if self.trait_registry.contains_key(&trait_name) {
                let type_arg = args.get(0).map(|v| v.to_string()).unwrap_or_default();
                return self.construct_trait_instance(
                    &trait_name, &trait_generics, &type_arg, &[], call_site
                );
            }
        } else {
            // : 
            let trait_name = qualified;
            if self.trait_registry.contains_key(trait_name) {
                let type_arg = args.get(0).map(|v| v.to_string()).unwrap_or_default();
                return self.construct_trait_instance(
                    trait_name, &[], &type_arg, &[], call_site
                );
            }
        }
    }
    // ... 
}
```

### 1.10 `parse_dyn_trait` `dyn Container<number>`

```rust
//  expression  DynTrait
Expr::DynTrait { generics, trait_name, span } => {
    // v0.09:  `dyn Container<number>` 
    self.eval_dyn_trait(trait_name, generics)
}
```

## 2. Typeck 

### 2.1 TraitDef 

```rust
// collect_signatures 
if let Stmt::TraitDef { name, generics, parents, methods, .. } = stmt {
    let trait_generics: Vec<String> = generics.iter().map(|g| g.name.clone()).collect();
    
    let mut method_sigs = Vec::new();
    for m in methods {
        let param_types: Vec<(String, Type)> = m.params.iter()
            .map(|(n, hint)| (n.clone(), hint.as_deref().map(Type::from_hint).unwrap_or(Type::Any)))
            .collect();
        let ret = m.return_type.as_deref().map(Type::from_hint).unwrap_or(Type::Any);
        let has_default = !m.body.is_empty();
        let has_self = m.params.first().map(|(n, _)| n == "self").unwrap_or(false);
        // v0.09: tuple  method_generics 
        method_sigs.push((
            m.name.clone(),
            Signature { params: param_types, return_type: ret },
            has_default,
            has_self,
            m.generics.iter().map(|g| g.name.clone()).collect(),  // v0.09
        ));
    }
    self.trait_registry.insert(name.clone(), TraitTypeDef {
        name: name.clone(),
        parents: parents.clone(),
        generics: trait_generics,  // v0.09
        methods: method_sigs,
    });
}
```

### 2.2 TraitTypeDef 

```rust
struct TraitTypeDef {
    name: String,
    parents: Vec<String>,
    /// v0.09: trait 
    generics: Vec<String>,
    /// (method_name, signature, has_default, has_self, method_generics)
    methods: Vec<(String, Signature, bool, bool, Vec<String>)>,
}
```

### 2.3 ImplDef 

```rust
if let Stmt::ImplDef {
    generics, trait_generics, trait_name, for_type,
    for_generics, where_clause, methods, ..
} = stmt {
    if !self.trait_registry.contains_key(trait_name) {
        self.errors.push(TypeError::from_span(span,
            format!("impl: trait '{}' not defined", trait_name)));
    } else {
        let trait_def = &self.trait_registry[trait_name];
        
        // v0.09: trait_generics 
        if trait_generics.len() != trait_def.generics.len() {
            self.errors.push(TypeError::from_span(span,
                format!("impl '{}' for '{}': trait expects {} generics, got {}",
                    trait_name, for_type, trait_def.generics.len(), trait_generics.len())));
        }
        
        // v0.09: where_clause bound  trait
        for w in where_clause {
            if let Some(bound) = &w.bound {
                if !self.trait_registry.contains_key(bound) {
                    self.errors.push(TypeError::from_span(span,
                        format!("impl '{}' for '{}': where bound '{}' is not a known trait",
                            trait_name, for_type, bound)));
                }
            }
        }
        
        // v0.09: 
        let mut all_methods: Vec<(String, Signature, bool, bool, Vec<String>)> = Vec::new();
        let mut visited = std::collections::HashSet::new();
        self.collect_trait_methods_recursive(trait_name, &mut visited, &mut all_methods);
        let impl_methods = methods.clone();
        for (tm_name, tm_sig, tm_has_default, _tm_has_self, _tm_method_generics) in &all_methods {
            // ...  / 
        }
    }
}
```

### 2.4 MethodCall on Trait 

```rust
if let Type::Trait { name: tname, generics: tg } = &ot {
    let mut all_methods: Vec<(String, Signature, bool, bool, Vec<String>)> = Vec::new();
    let mut visited = std::collections::HashSet::new();
    self.collect_trait_methods_recursive(tname, &mut visited, &mut all_methods);
    for (mname, sig, _has_default, has_self, _method_generics) in &all_methods {
        if mname == method {
            // v0.09:  v0.08.5 has_self  1
            let expected = if *has_self { sig.params.len().saturating_sub(1) } else { sig.params.len() };
            if args.len() != expected {
                self.errors.push(TypeError::from_span(span,
                    format!("trait method '{}.{}' expects {} args, got {}",
                        tname, method, expected, args.len())));
            }
            return sig.return_type.clone();
        }
    }
}
```

### 2.5 collect_trait_methods_recursive 

```rust
fn collect_trait_methods_recursive(
    &self,
    trait_name: &str,
    visited: &mut std::collections::HashSet<String>,
    out: &mut Vec<(String, Signature, bool, bool, Vec<String>)>,  // v0.09:  method_generics
) {
    // ... 
    for m in &td.methods {
        out.retain(|(n, _, _, _, _)| n != &m.0);
        out.push(m.clone());
    }
}
```

## 3. 

### 3.1 trait 3 

#### `test_trait_generic_basic`
```mora
trait Container<T>
  fn get(self) -> T
end

impl Container<number> for BoxedNum
  fn get(self) = 42
end

task main()
  let c: dyn Container<number> = Container<number>::new("BoxedNum")
  print(c.get())
end
```
**** `42`

#### `test_trait_generic_multi_param`
```mora
trait Pair<A, B>
  fn first(self) -> A
  fn second(self) -> B
end

impl Pair<string, number> for NameAge
  fn first(self) = "Alice"
  fn second(self) = 30
end

task main()
  let p: dyn Pair<string, number> = Pair<string, number>::new("NameAge")
  print(p.first())
  print(p.second())
end
```
**** `Alice` / `30`

#### `test_trait_generic_nested`
```mora
trait Boxed<T>
  fn unbox(self) -> T
end

trait Container<T>
  fn get(self) -> T
end

impl Container<Boxed<number>> for Outer
  fn get(self) = self.inner
end

task main()
  let c: dyn Container<Boxed<number>> = Container<Boxed<number>>::new("Outer")
  let inner = c.get()
  print(inner.unbox())  --  impl Boxed<number> for ???
end
```
**** `Boxed<number>`  value 
```mora
impl Boxed<number> for BoxedNum
  fn unbox(self) = 99  -- 
end
```

### 3.2 impl 2 

#### `test_impl_generic_concrete`
```mora
trait Container<T>
  fn get(self) -> T
end

impl Container<number> for BoxedNum
  fn get(self) = 42
end

impl Container<string> for BoxedStr
  fn get(self) = "hello"
end

task main()
  let n: dyn Container<number> = Container<number>::new("BoxedNum")
  let s: dyn Container<string> = Container<string>::new("BoxedStr")
  print(n.get())
  print(s.get())
end
```
**** `42` / `hello`
**** trait  dispatch key

#### `test_impl_generic_generic_for_generic`
```mora
trait Container<T>
  fn get(self) -> T
end

impl<T> Container<T> for Wrapper<T>
  fn get(self) = self.value
end

task main()
  let w: Wrapper<number> = Wrapper<number> { value: 42 }
  let c: dyn Container<number> = Container<number>::new("Wrapper")
  print(c.get())
end
```
**** `42`
**** Wrapper<T> v0.08  struct literal——v0.09  value
```mora
--  struct literal Wrapper  builtin
```

### 3.3 where 1 

#### `test_where_clause_basic`
```mora
trait Comparable
  fn less(self, other: Self) -> bool
end

trait Container<T> where T: Comparable
  fn min(self, other: T) -> T
end

impl Container<number> for NumPair
  fn min(self, other) = if self < other then self else other
end

task main()
  let p: dyn Container<number> = Container<number>::new("NumPair")
  print(p.min(5))
end
```
**** `5`
****v0.09 where  bound trait  Comparable  impl

### 3.4 typeck 3 

#### `test_typeck_error_trait_generics_mismatch`
```mora
trait Container<T>  -- 1  generic
end

impl Container for Boxed  --  1  0
end
```
****`impl 'Container' for 'Boxed': trait expects 1 generics, got 0`

#### `test_typeck_error_where_unknown_trait`
```mora
trait Container<T> where T: NonExistent  -- NonExistent  trait
end
```
****`trait 'Container' has unknown bound 'NonExistent'`

#### `test_typeck_error_dispatch_missing_impl`
```mora
trait Container<T>
  fn get(self) -> T
end

--  impl Container<number> for BoxedNum
task main()
  let c: dyn Container<number> = Container<number>::new("BoxedNum")
  print(c.get())  -- no impl
end
```
****`trait dispatch at line N: no impl for type 'BoxedNum' method 'get'`

### 3.5 e2e  case3  test_data/typeck_errors.mora

```mora
-- Case 28: trait 
trait ContainerA<T>
  fn get(self) -> T
end

impl ContainerA for BoxedA  --  <T>
  fn get(self) = 1
end

-- Case 29: impl 
trait ContainerB<T>
  fn get(self) -> T
end

impl ContainerB<number, string> for BoxedB  --  1 
  fn get(self) = 1
end

-- Case 30: where  unknown bound
trait ContainerC<T> where T: NonExistentBound
  fn get(self) -> T
end
```

### 3.6 

|  |  |
|------|------|
| trait  | 3 |
| impl  | 2 |
| where | 1 |
| typeck  | 3 |
| e2e  | 3 |
| **** | **12** |
| **** | **57** |
| **** | **69** |

## 4. Examples 3 

### 4.1 `examples/container.mora`

```mora
-- v0.09:  Container trait demo
-- : trait  1 impl 

trait Container<T>
  fn get(self) -> T
  fn set(self, value: T)
end

impl Container<number> for BoxedNum
  fn get(self) = 42
  fn set(self, value) = print("set " + value)
end

impl Container<string> for BoxedStr
  fn get(self) = "hello"
  fn set(self, value) = print("set " + value)
end

task main()
  let n: dyn Container<number> = Container<number>::new("BoxedNum")
  print(n.get())  -- 42
  n.set(100)
  let s: dyn Container<string> = Container<string>::new("BoxedStr")
  print(s.get())  -- hello
end
```

### 4.2 `examples/generic_with_where.mora`

```mora
-- v0.09:  + where  demo
-- : trait  + impl where 

trait Comparable
  fn less(self, other: Self) -> bool
end

trait Min<T> where T: Comparable
  fn min(self, other: T) -> T
end

impl Min<number> for NumPair
  fn min(self, other) = if self.less(other) then self else other
end

task main()
  let p: dyn Min<number> = Min<number>::new("NumPair")
  print(p.min(5))   -- 5
  print(p.min(10))  -- 5 (smaller)
end
```

### 4.3 `examples/nested_generic.mora`

```mora
-- v0.09:  demo
-- : trait  trait 

trait Boxed<T>
  fn unbox(self) -> T
end

trait Container<T>
  fn get(self) -> T
end

impl Boxed<number> for NumBox
  fn unbox(self) = 42
end

impl Container<Boxed<number>> for Outer
  fn get(self) = NumBox { }  -- v0.09 
end

task main()
  let c: dyn Container<Boxed<number>> = Container<Boxed<number>>::new("Outer")
  let inner = c.get()
  print(inner.unbox())  -- 42
end
```

### 4.4 examples 

```bash
#  12  examples  typeck + 
for f in examples/*.mora; do
  echo "=== $f ==="
  ./target/release/mora --check "$f" 2>&1 | tail -1
  ./target/release/mora "$f" 2>&1
done
```

## 5. 

### 5.1 dispatch key 

****`no impl for type 'X' method 'Y'`

****
1.  `impl_table`  `__impl_<Trait>_<TraitGen>_<ForType>_<ForGen>_<method>`
2.  dispatcher  key  impl_table  key
3.  generics `"number"` vs `"Number"`

****
```rust
//  execute(Stmt::ImplDef) 
eprintln!("Registered: {}", impl_method_name);
```

### 5.2 where 

****`where bound 'X' is not a known trait`

****where  bound  trait 

**** trait  trait_registry 

### 5.3  TraitObject 

****dispatch  TraitObject  for_generics / trait_generics 

****eval_dyn_trait  construct_trait_instance  generics

**** dispatch_trait_method 
```rust
eprintln!("Dispatch: for_type={}, for_generics={:?}, trait_name={}, trait_generics={:?}",
    for_type, for_generics, trait_name, trait_generics);
```

## 6. 

```bash
# 1. 
cd d:/Github/mora-lang
cargo build --release

# 2. 
cargo test --lib --release
# : 69 passed (57  + 12 )

# 3. e2e
bash test_data/verify_typeck_errors.sh
# : 22/22 19  + 3  trait 

# 4.  examples
for f in examples/*.mora; do
  echo "=== $f ==="
  ./target/release/mora "$f" 2>&1 | tail -3
done
# :  12  examples 

# 5. trait_inherit_demo v0.08 
./target/release/mora examples/trait_inherit_demo.mora
# : name: Alice / age: 30 / greet: Hello, I am Alice, age 30
```

## 7. 

### 7.1 

|  |  |  |
|------|------|------|
| generics  | `Foo<T>`  |  lookahead |
| dispatch key  |  trait  impl |  type name  |
| TraitObject 4  | v0.08 TraitObject  |  |
| where  |  | v0.09  |
| Default trait  | v0.08 TraitObject literal  | `..Default::default()`  |

### 7.2 

** v0.09 **
1.  where  v0.10
2.  trait/impl  v0.09.0-alpha
3.  commit  v0.09.1 / v0.09.2

** commit **
```bash
#  v0.09  commit
git commit -m "v0.09.0-alpha: trait  + impl  where"
```

## 8. 

- [ ] v0.08  57  + 9 examples + 19 e2e 
- [ ] v0.09  9 3 trait  + 2 impl  + 1 where + 3 typeck 
- [ ] v0.09  3  e2e  case 
- [ ] v0.09  3  examplescontainer / generic_with_where / nested_generic
- [ ]  >= 69
- [ ] verify_typeck_errors.sh >= 22/22 
- [ ] 12 examples  typeck + 
- [ ] docs/v0.09-implementation-workflow.md 
- [ ] v0.09 commit message `v0.09: trait  + impl  + where`

---

****Mora 
****2026-06-20
**License**BSD-3-Clause
****
- `docs/v0.09-design.md`
- `docs/v0.09-impl-spec.md`AST/Type/Parser 
- `docs/v0.08-trait-system-workflow.md`v0.08 trait 
- `docs/v0.08.5-implementation-workflow.md`v0.08.5 trait hardening