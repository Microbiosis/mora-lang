# Mora v0.10 

commit `9c32a86 v0.09: trait `
 v0.10 commit
2026-06-20

> ****v0.09 trait "v0.09 "
> v0.10  Mora ——**AI ** + Retry

## 0. 

v0.09  Mora AI-native scripting languagev0.10  **AI **—— retry

|  | commit |  |
|------|--------|----------|
| v0.09 | `9c32a86` | trait 3  / 1879 **** |
| v0.10 |  commit | AI  retryexponential backoff + jitter |

## 1. v0.10 

| # | Finding |  |
|---|---------|---------|
| F1 | AI  retry | retry  3  + base 1000ms  + ±20% jitter |
| F2 |  | ureq::Error::Transport  retry |
| F3 |  | HTTP 5xx  retry |
| F4 | Rate limit  | HTTP 429  retry |
| F5 | retry  |  `MORA_AI_RETRY_MAX` 3 |
| F6 | retry  |  `MORA_AI_RETRY_BASE_MS` 1000 |

## 2. src/interpreter.rs

### 2.1 3  helper 

```rust
/// v0.10: retry  3
fn ai_retry_max() -> u32 { /* MORA_AI_RETRY_MAX env var */ }

/// v0.10: retry  1000ms
fn ai_retry_base_ms() -> u64 { /* MORA_AI_RETRY_BASE_MS env var */ }

/// v0.10: 
///   - Transport
///   - HTTP 429 rate limit
///   - HTTP 5xx server error
fn is_retryable_error(err: &str) -> bool

/// v0.10: retry sleep  + jitter
///   attempt=0 → base
///   attempt=1 → base * 2 + jitter
///   attempt=2 → base * 4 + jitter
fn retry_sleep_ms(attempt: u32, base_ms: u64) -> u64
```

### 2.2 real_ai_chat_inner retry 

```rust
// v0.10: retry exponential backoff + jitter
let max_retries = ai_retry_max();
let base_ms = ai_retry_base_ms();
for attempt in 0..=max_retries {
    if attempt > 0 {
        let sleep = retry_sleep_ms(attempt - 1, base_ms);
        std::thread::sleep(Duration::from_millis(sleep));
    }
    match agent.post(&url) /* ... */ {
        // return
        // if is_retryable_error && attempt < max_retries → continue
        //       else → return Err
    }
}
```

## 3. 

|  | v0.09 | v0.10 |
|------|-------|------|
|  | 57 | 65 (+8) |
|  |  |  |
| e2e  | 19/19 | 19/19 |
| typeck errors | 28 | 28 |
| 9 examples |  |  |
| AI  retry |  |  |

## 4.  8  retry 

`is_retryable_error`
- `test_is_retryable_error_transport` — 
- `test_is_retryable_error_http_429` — 429 rate limit 
- `test_is_retryable_error_http_5xx` — 500/502/503/599 
- `test_is_retryable_error_http_4xx_not_retryable` — 400/401/403/404 
- `test_is_retryable_error_other` — 

`retry_sleep_ms`
- `test_retry_sleep_ms_exponential` — 
- `test_retry_sleep_ms_no_overflow` — 

`ai_retry_*`
- `test_ai_retry_config_defaults` —  panic

## 5. 

|  |  |  |
|------|------|------|
| `MORA_AI_RETRY_MAX` | 3 |  |
| `MORA_AI_RETRY_BASE_MS` | 1000 |  + ±20% jitter |


```bash
# 4  1+2+4 = 7 
./mora script.mora

#  retry0 
MORA_AI_RETRY_MAX=0 ./mora script.mora

#  retry5 base 100ms
MORA_AI_RETRY_MAX=5 MORA_AI_RETRY_BASE_MS=100 ./mora script.mora
```

## 6. 

### v0.09
-  → AI mora script 
-  500  → 
- 429 rate limit →  retry

### v0.10
-  →  retry 3  1s/2s/4s + jitter 7+ 
-  500 →  retry
- 429 rate limit →  retry
- HTTP 4xx 429→ client  retry 
-  retry  →  retry 

## 7. 

README 
>  AI `p"..."` HTTP serverMCP serverAgent 

v0.10 retry **" AI "**——`p"..."`  Mora 

## 8. v0.11 


1. ****`p"text @image.png"` 
2. **** SSE 
3. ****fallback modelcircuit breaker
4. **prompt **`prompt "system: <role>: " {ctx}`

 1 + 3 "AI "——

---

****2026-06-20
****Mora 
**License**BSD-3-Clause
****`docs/v0.09-design.md` / `docs/v0.09-impl-spec.md` / `docs/v0.09-test-dispatch-spec.md`
