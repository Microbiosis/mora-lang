# Mora v0.11 

commit `9f8742f v0.09: trait  + impl  + where `
 v0.11 commit
2026-06-21

> ****v0.10 AI-native scripting language
> v0.11  v0.10  commit——**HTTP server **

## 0. 

|  | commit |  |
|------|--------|----------|
| v0.10 | `393fb1a` | AI  retryexponential backoff + jitter |
| v0.11 |  commit | HTTP server SO_REUSEADDR +  |

## 1. 

**v0.10 **`TcpListener::bind(&addr)?`  io::Error server 


1. ** demo ** server TIME_WAIT  demo →  `os error 10048`Windows `os error 98`Linux
2. ** demo **5  examples  4096  →  →  4 
3. **TIME_WAIT**TCP  30-120 MSL TIME_WAIT 

## 2. 

| # | Finding |  |
|---|---------|---------|
| F1 |  panic | 4096, 4097, 4098, 4099 |
| F2 | TIME_WAIT  |  SO_REUSEADDR libc::setsockopt |
| F3 | Windows/Linux  | SOL_SOCKET=1 Unix / 0xFFFF WindowsSO_REUSEADDR=2/4 |
| F4 |  |  eprintln `requested port N unavailable, using M instead` |
| F5 | README  | "v0.11 " |

## 3. src/http_server.rs

### 3.1 SO_REUSEADDR 

```rust
fn set_reuse_addr(listener: &TcpListener) {
    #[cfg(unix)]
    {
        use std::os::unix::io::AsRawFd;
        let fd = listener.as_raw_fd();
        let opt: libc::c_int = 1;  // enable
        let _ = unsafe {
            libc::setsockopt(fd, 1 /*SOL_SOCKET*/, 2 /*SO_REUSEADDR*/,
                &opt as *const _ as *const libc::c_void,
                std::mem::size_of_val(&opt) as libc::socklen_t)
        };
    }
    #[cfg(windows)]
    {
        use std::os::windows::io::AsRawSocket;
        let socket = listener.as_raw_socket();
        let opt: libc::c_int = 1;
        let _ = unsafe {
            libc::setsockopt(socket as libc::SOCKET, 0xFFFF /*SOL_SOCKET*/, 4 /*SO_REUSEADDR*/,
                &opt as *const _ as *const libc::c_char,
                std::mem::size_of_val(&opt) as libc::c_int)
        };
    }
}
```

### 3.2 

```rust
fn bind_with_reuse(host: &str, requested_port: u16, max_attempts: u16) -> io::Result<(TcpListener, u16)> {
    for offset in 0..max_attempts {
        let port = match requested_port.checked_add(offset) {
            Some(p) => p,
            None => break,
        };
        match TcpListener::bind(format!("{}:{}", host, port)) {
            Ok(listener) => {
                set_reuse_addr(&listener);
                if offset > 0 {
                    eprintln!("[serve] requested port {} unavailable, using {} instead",
                        requested_port, port);
                }
                return Ok((listener, port));
            }
            Err(_) => continue,  // 
        }
    }
    Err(io::Error::new(io::ErrorKind::AddrInUse,
        format!("could not bind to any port in range {}-{}",
            requested_port, requested_port.saturating_add(max_attempts - 1))))
}
```

### 3.3 start 

```rust
pub fn start(host: &str, port: u16, ...) -> io::Result<()> {
    // v0.11: 
    let (listener, actual_port) = bind_with_reuse(host, port, 4)?;
    let addr = format!("{}:{}", host, actual_port);
    eprintln!("[serve] Mora HTTP server listening on http://{}", addr);
    // ... 
}
```

### 3.4 Cargo.toml 

```toml
libc = { version = "0.2", features = ["extra_traits"] }
```

`extra_traits` feature  SOL_SOCKET / SO_REUSEADDR  fallback feature 

## 4. 

|  | v0.10 | v0.11 |
|------|-------|------|
|  | 65 | 65 |
|  |  |  |
| e2e  | 19/19 | 19/19 |
| typeck errors | 32 | 32 |
| 9 examples |  |  |
|  |   |   |
| SO_REUSEADDR |  |  TIME_WAIT  |
| README  |  |  "v0.11 " |

### 

#### v0.10
```
$ ./mora examples/bench_server.mora
[Router] starting HTTP server on 127.0.0.1
thread 'main' panicked at 'os error 10048: ...
```

#### v0.11
```
$ ./mora examples/bench_server.mora
[Router] starting HTTP server on 127.0.0.1
[serve] requested port 3000 unavailable, using 3001 instead
[serve] Mora HTTP server listening on http://0.0.0.0:3001
[serve] Endpoints:
  GET /health
  ...
```

#### 3  server 

```bash
$ ./mora bench1.mora &  #  3000
$ ./mora bench2.mora &  #  3001
$ ./mora bench3.mora &  #  3002

$ netstat -ano | grep :300
  TCP    0.0.0.0:3000    LISTENING    pid1
  TCP    0.0.0.0:3001    LISTENING    pid2
  TCP    0.0.0.0:3002    LISTENING    pid3
```

## 5. 

|  |  |  |
|------|------|------|
|  libc | libc 0.2 |  socket2 ureq  |
| SO_REUSEADDR  |  1/2/0xFFFF/4 | extra_traits feature  libc  |
| max_attempts | 4 | 4  |
|  | eprintln  | SO_REUSEADDR  best-effort |
|  vs  |  |  |

## 6.  commit

|  |  |
|------|------|
|  | `MORA_HTTP_PORT` / `MORA_HTTP_STRICT_PORT`  |
|  | `mimocode.json` strict / auto-bump / random |
|  |  |
| IPv6  |  IPv4 |

## 7. 

README 
>  AI `p"..."` HTTP serverMCP serverAgent 

HTTP server ——****

---

****2026-06-21
****Mora 
**License**BSD-3-Clause
