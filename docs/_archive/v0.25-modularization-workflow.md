# v0.25 

>  Mora  v0.25 

## 

1. [](#1-)
2. [](#2-)
3. [](#3-)
4. [interpreter ](#4-interpreter-)
5. [typeck ](#5-typeck-)
6. [parser_v2 ](#6-parser_v2-)
7. [record ](#7-record-)
8. [lsp/providers ](#8-lspproviders-)
9. [CI/CD ](#9-cicd-)
10. [](#10-)
11. [](#11-)

---

## 1. 

### 

 2000 

|  |  |  |
|------|------|------|
| `src/interpreter/mod.rs` | 3402 |  |
| `src/typeck.rs` | 2838 |  |
| `src/parser_v2.rs` | 2609 |  |
| `src/record.rs` | 2091 |  |
| `src/lsp/providers.rs` | 1092 | 9  LSP  |

### 

- 
- 
- 
- 

---

## 2. 

### 

```bash
find . -name "*.rs" -type f -exec wc -l {} \; | sort -rn | head -20
```


```
3402 ./src/interpreter/mod.rs
2838 ./src/typeck.rs
2609 ./src/parser_v2.rs
2091 ./src/record.rs
1092 ./src/lsp/providers.rs
```

### 

 `grep` 

```bash
grep -n "^pub fn\|^fn\|^pub struct\|^struct\|^pub enum\|^enum\|^impl\|^pub trait\|^trait" src/interpreter/mod.rs | head -50
```

### 

```bash
#  execute 
sed -n '578,1160p' src/interpreter/mod.rs | wc -l
# : 583
```

### 



---

## 3. 

### 

1. ****
2. **** `pub(super)`  `pub` 
3. ****
4. ****

### Rust 

Rust  `impl` 

```rust
// src/parser_v2/mod.rs
pub struct ParserV2 {
    tokens: Vec<Token>,
    current: usize,
    arena: AstArena,
}

// src/parser_v2/statements.rs
use super::*;
impl ParserV2 {
    pub(super) fn let_declaration_exported(...) { ... }
}

// src/parser_v2/expressions.rs
use super::*;
impl ParserV2 {
    pub(super) fn expression(...) { ... }
}
```

---

## 4. interpreter 

### 4.1 

```bash
#  Interpreter 
sed -n '360,1565p' src/interpreter/mod.rs | grep -n "pub fn\|fn " | head -30
```


- `execute` (585 ) - 
- `evaluate` (207 ) - 
- `match_pattern` (100 ) - 

### 4.2  execute.rs



```rust
// src/interpreter/execute.rs
use super::*;
use crate::ast_v2::{AstArena, NodeId, StmtKind};
use crate::value::{Environment, FlowSignal, Value};

impl Interpreter {
    /// 
    pub fn execute(
        &mut self,
        stmt_kind: &StmtKind,
        arena: &AstArena,
    ) -> Result<FlowSignal, String> {
        match stmt_kind {
            StmtKind::Let { name, init, exported, .. } => {
                self.execute_let(name, *init, *exported, arena)
            }
            // ... 
        }
    }

    ///  let 
    fn execute_let(
        &mut self,
        name: &str,
        init: NodeId,
        exported: bool,
        arena: &AstArena,
    ) -> Result<FlowSignal, String> {
        let value = self.evaluate(init, arena)?;
        self.environment
            .lock()
            .expect("environment mutex poisoned")
            .define(name.to_string(), value, exported);
        Ok(FlowSignal::None)
    }
    // ... 
}
```

### 4.3  evaluate.rs



```rust
// src/interpreter/evaluate.rs
use super::*;
use crate::ast_v2::{AstArena, ExprKind, NodeId, Pattern};

impl Interpreter {
    ///  ast_v2 
    pub fn evaluate(
        &mut self,
        expr_id: NodeId,
        arena: &AstArena,
    ) -> Result<Value, String> {
        let expr = arena.get_expr(expr_id).ok_or("Invalid expression ID")?;
        match &expr.kind {
            ExprKind::Literal(lit) => self.literal_to_value_inner(lit),
            ExprKind::Variable(name) => { ... }
            // ... 
        }
    }

    /// 
    pub(super) fn match_pattern(
        &mut self,
        pattern: &Pattern,
        value: &Value,
        arena: &AstArena,
    ) -> Option<Vec<(String, Value)>> {
        // ...
    }
}
```

### 4.4  mod.rs

 `mod.rs` 

```rust
mod execute;
mod evaluate;
```



### 4.5 

```bash
cargo check
cargo test --lib
cargo clippy --all-targets --all-features -- -D warnings
```

---

## 5. typeck 

### 5.1 

```bash
mkdir -p src/typeck
mv src/typeck.rs src/typeck/mod.rs
```

### 5.2  check.rs



```rust
// src/typeck/check.rs
use super::*;
use crate::ast_v2::{AstArena, ExprKind, NodeId, StmtKind};

impl TypeChecker {
    /// 
    pub fn check_stmt(
        &mut self,
        kind: &StmtKind,
        arena: &AstArena,
        symbols: &mut SymbolTable,
    ) {
        match kind {
            StmtKind::Let { name, type_hint, init, .. } => {
                self.check_let_stmt(name, type_hint.as_deref(), *init, arena, symbols);
            }
            // ...
        }
    }

    /// 
    pub fn check_expr(
        &mut self,
        expr_id: NodeId,
        arena: &AstArena,
        symbols: &SymbolTable,
    ) -> Type {
        // ...
    }
}
```

### 5.3  mod.rs

```rust
mod check;

// 
```

---

## 6. parser_v2 

### 6.1 

```bash
mkdir -p src/parser_v2
mv src/parser_v2.rs src/parser_v2/mod.rs
```

### 6.2 

```bash
#  161-1640
git show HEAD:src/parser_v2.rs | sed -n '161,1640p' > /tmp/stmt_body.rs
```

 `statements.rs`

```rust
// src/parser_v2/statements.rs
use super::*;

impl ParserV2 {
    pub(super) fn let_declaration_exported(&mut self, exported: bool) -> NodeId {
        // ...
    }

    pub(super) fn task_declaration_exported(&mut self, exported: bool) -> NodeId {
        // ...
    }
    // ...
}
```

### 6.3 

```bash
#  1646-2122
git show HEAD:src/parser_v2.rs | sed -n '1646,2122p' > /tmp/expr_body.rs
```

 `expressions.rs`

```rust
// src/parser_v2/expressions.rs
use super::*;

impl ParserV2 {
    pub(super) fn expression(&mut self) -> NodeId {
        self.pipe()
    }

    fn pipe(&mut self) -> NodeId { ... }
    fn binary(&mut self) -> NodeId { ... }
    // ...
}
```

### 6.4 

 `pub(super)`

```rust
// 
fn expression(&mut self) -> NodeId { ... }

//  pub(super) 
pub(super) fn expression(&mut self) -> NodeId { ... }
```

---

## 7. record 

### 7.1 

record.rs 
- /
- 
- /
- 
- 

### 7.2 

```
src/record/
 mod.rs           (315)  + Recorder
 serialization.rs (323) JSONL 
 diff.rs          (92)  
 analysis.rs      (294) //
 audit.rs         (192) 
 snapshot.rs      (284) 
 tests.rs         (606) 
```

### 7.3 

 `pub(super)`

```rust
// src/record/serialization.rs
pub(super) fn event_to_jsonl(ev: &Event) -> String { ... }
pub(super) fn load_jsonl(path: &Path) -> Result<Vec<Event>, String> { ... }
```



```rust
// src/record/analysis.rs
use super::serialization::{load_jsonl, event_to_jsonl};
```

### 7.4 

 `#[cfg(test)]` 

```rust
// src/record/mod.rs
#[cfg(test)]
mod tests;

// src/record/tests.rs
use crate::record::*;
use std::env;
use std::fs;

fn tmp_path(name: &str) -> PathBuf { ... }

#[test]
fn record_roundtrip() { ... }
```

---

## 8. lsp/providers 

### 8.1  LSP 

```
src/lsp/providers/
 mod.rs        (22)   
 helpers.rs    (222)  
 hover.rs      (84)   hover provider
 completion.rs (79)   completion provider
 definition.rs (76)   definition provider
 references.rs (76)   references provider
 symbols.rs    (119)  document symbols
 formatting.rs (150)  formatting
 rename.rs     (97)   rename
 semantic.rs   (132)  semantic tokens
 folding.rs    (83)   folding range
```

### 8.2 

 JSON  serde_json

```rust
// 
use serde_json::Value;

// 
use crate::lsp::json::Value;
```

### 8.3  BTreeMap/BTreeSet 



```rust
use std::collections::{BTreeMap, BTreeSet, HashMap};
```

---

## 9. CI/CD 

### 9.1 rustfmt 

**** rustfmt 

****
```bash
cargo fmt -- src/interpreter/mod.rs src/interpreter/execute.rs ...
```

### 9.2 Windows 

**** `/tmp/`  Windows 

****
```
memory save/load should work: "memory.save: The system cannot find the path specified. (os error 3)"
```

**** `std::env::temp_dir()` 

```rust
#[test]
fn test_memory_save_load() {
    use std::env;
    let tmp_dir = env::temp_dir();
    let tmp_file = tmp_dir.join("mora_test_memory.json");
    let tmp_path = tmp_file.to_string_lossy().replace('\\', "/");
    let src = format!(r#"
task main()
  memory.store("key1", "value1")
  memory.save("{}")
end
"#, tmp_path);
    run(&src).expect("memory save/load should work");
    let _ = std::fs::remove_file(tmp_file);
}
```

### 9.3 Docker 

****Dockerfile  Rust 1.86 edition 2024

**** Dockerfile  Rust 

```dockerfile
# 
FROM rust:1.86-alpine AS builder

# 
FROM rust:alpine AS builder
```

### 9.4 Release body 

****Release body  `docs/` 

**** `.github/workflows/release.yml`

### 9.5 Docker  Release 

****`release` job  `build` `docker`

****

```yaml
release:
    name: Publish GitHub Release
    needs: [build, docker]  #  docker 
```

---

## 10. 

### 10.1 

```bash
# Cargo.toml
version = "0.0.25"

# src/main.rs
println!("Mora v0.25");
```

### 10.2  CHANGELOG

```markdown
## [v0.25] - 2026-07-01

### 

- interpreter: 3402  → 3 
- typeck: 2838  → 2 
- parser_v2: 2609  → 3 
- record: 2091  → 7 
- lsp/providers: 1092  → 11 
```

### 10.3 



```bash
# 
git reset --soft <commit-hash>

# 
git commit -m "feat: v0.25 "
```

### 10.4  Release

```bash
#  tag
git tag -d v0.25
git push origin :refs/tags/v0.25

#  tag
git tag v0.25
git push origin v0.25
```

### 10.5 

 AI 

```bash
#  .gitignore
echo "CLAUDE.md" >> .gitignore
echo "AGENTS.md" >> .gitignore

#  git 
git rm --cached CLAUDE.md AGENTS.md
```

---

## 11. 

### 11.1 

1. ****
2. ****
3. ****
4. ****

### 11.2 

|  |  |  |
|------|------|----------|
|  |  |  `pub(super)` |
|  |  |  `use super::*`  `use crate::...` |
|  |  |  `cargo fmt` |
|  |  |  API |

### 11.3 

- `wc -l`
- `grep -n`
- `sed -n 'start,endp'`
- `cargo check`
- `cargo clippy`
- `cargo fmt`
- `gh`GitHub CLI Release

### 11.4 

|  |  |  |  |
|------|--------|--------|--------|
| interpreter | 3402  | 2504  | 3 |
| typeck | 2838  | 2006  | 2 |
| parser_v2 | 2609  | 645  | 3 |
| record | 2091  | 315  | 7 |
| lsp/providers | 1092  | 22  | 11 |
| **** | **12032 ** | **5492 ** | **26 ** |

---

## 

```bash
# 
wc -l src/**/*.rs | sort -rn | head -20
grep -n "pub fn\|fn " src/file.rs

# 
cargo check
cargo test --lib
cargo clippy --all-targets --all-features -- -D warnings

# 
cargo fmt
cargo fmt --check

# Git 
git reset --soft <commit>
git commit --amend --no-edit
git push --force

# Release 
gh release view v0.25
gh release delete v0.25 --yes
git tag -d v0.25
git push origin :refs/tags/v0.25
```
