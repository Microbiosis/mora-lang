# Mora v0.07 实施工作流总结（2026-06-18）

起点：commit `3666801 v0.06.6: McpServer 显式 API`
终点：commit `5b2e731 v0.07.2: 更新 examples 为显式 API + 修复 consume_method_name 关键字兼容`
日期：2026-06-18

> **承接规划**：`docs/v0.06-implementation-workflow-final.md` 第 9 章（已知遗留）
> **关联文档**：`docs/v0.05-to-v1.0-modernization-plan.md`（v0.07 部分参考）

---

## 0. 摘要

v0.07 分三步提交，从 v0.06.6 直接跳到 v0.07——核心动机是**不再对旧语法做向后兼容**。

v0.06 留下了 5 个已知遗留项，v0.07 全部处理完毕：

| 遗留项 | v0.07.0 | v0.07.1 | v0.07.2 |
|--------|---------|---------|---------|
| try-catch 糖移除 | ✅ | — | — |
| serve as 糖移除 | ✅ | — | — |
| req.json() | — | ✅ | ✅ |
| e2e 验证脚本 | — | ✅ | — |
| Benchmark | — | ✅ | — |
| examples 重写为显式 API | — | — | ✅ |

- **3 个 commit**，push 到 origin/main
- **27 个文件改动**（9 源码 + 3 example 新增 + 3 example 删除 + 2 test_data + 1 docs + 2 自动生成）
- **+443 行, -636 行，净减 ~193 行**
- **47 个单元测试**全部通过（0 回归）
- **23 个 e2e typeck 错误**命中（类别 11 try-catch 已废弃）
- **6 个 examples** 全部 typeck 通过（3 个旧 serve_as 文件已替换）
- **v0.07_validation.mora** — 新增 12 项 typeck 验证 0 错误

---

## 1. 起点回顾（v0.06.6 完成态）

v0.06.6 完成态（`3666801`）：
- 源码：~9900 行 Rust
- typeck 支持 22 种类型，Value 支持 17 种
- Router / McpServer / AiConfig / Result<T,E> / ? 均已实现
- 但 **try-catch** 和 **serve as** 两个旧语法仍然作为 `Stmt::Try` / `Stmt::Serve` 独立处理
- examples 中的 `serve_as_http_demo.mora` / `serve_as_mcp_demo.mora` / `serve_with_route_demo.mora` 均使用旧语法

v0.06 遗留了文档中明确标注的 5 项**不向下兼容**的改动，需要在下一版清理。

---

## 2. v0.07 总体设计

### 2.1 核心转向：不向后兼容

| 现状 (v0.06) | v0.07 |
|--------------|-------|
| `try-catch` 解析为 `Stmt::Try`，解释器独立分支 | **移除**。错误处理走 `Result<T,E>` + `?` + `match` |
| `serve as http/mcp/repl/stdio` 解析为 `Stmt::Serve` | **移除**。云服务走 `Router::new()` / `McpServer::new()` 显式 API |
| `req.body` 手动 `json.parse` | `req.json()` 一行搞定，返回 `Result<Value, ParseError>` |
| `Router::new()` 在解释器中但**无 :: 语法** | `::` 作为一等 token (`ColonColon`)，`Router::new()` 可被解析器正确识别 |
| 无 e2e 验证 | `test_data/v0.07_validation.mora` 12 项 typeck 验证 |
| 3 个旧 examples 使用 serve as 语法 | 3 个新 examples 全部改写为 Router::new() / McpServer::new() 显式 API |

### 2.2 保留的独立 API

以下 **不是糖，是独立功能**，不受影响：
`route` / `observe` / `span` / `with` / `stream` / `tool` / `record_tokens`

---

## 3. Commit 清单

| Commit | 内容 | 文件 | +/- |
|--------|------|------|-----|
| `57a7a80` | v0.07.0: 移除向后兼容旧语法糖 | 12 | +36 / -512 |
| `5a8b62f` | v0.07.1: req.json() + :: 语法 + e2e 验证脚本 + Benchmark | 9 | +283 / -3 |
| `5b2e731` | v0.07.2: 更新 examples 为显式 API + 修复 consume_method_name 关键字兼容 | 7 | +124 / -121 |
| **合计** | | **28** | **+443 / -636** |

---

## 4. 各 commit 详细说明

### 4.1 v0.07.0: 移除向后兼容旧语法糖

**目标**：彻底删除 try-catch 和 serve as 的底层支持，不再保留 Stmt 中间表示。

**改动**：

- `src/lexer.rs`：TokenType 枚举移除 `Try`/`Catch`/`Serve`/`Http`/`Mcp`/`Repl`/`Stdio`/`On`，降级为普通 Identifier
- `src/ast.rs`：删除 `Stmt::Try` / `Stmt::Serve` / `ServeProtocol` / `RouteDecl` / `HttpMethod` 及 impl
- `src/parser.rs`：删除 `try_statement()` / `serve_statement()` / `is_http_route_line()` / `is_mcp_tool_line()` / `parse_http_route()` / `parse_mcp_tool_route()` 共 ~180 行
- `src/interpreter.rs`：删除 `Stmt::Try` 执行分支 / `Stmt::Serve` 执行分支 / `serve_stdio_echo()` / `build_ai_error_static()` / `return_in_try_does_not_trigger_catch` 测试
- `src/typeck.rs`：删除 `Stmt::Try` typeck 分支 / `Stmt::Serve` typeck 分支（含 port 校验 / route handler 校验）
- `src/lsp/providers.rs`：删除 Try/Serve 相关的 walk_stmt / walk_expr / completion / folding 分支
- `src/http_server.rs`：移除 `HttpMethod` import，更新注释
- `src/main.rs`：banner 更新为 v0.07，移除 `catch e: AiError` / `serve` 等关键字
- `Cargo.toml`：version `0.0.6` → `0.0.7`
- `test_data/typeck_errors.mora`：类别 11 try-catch 测试改为注释废弃

**验收**：
- `cargo build` ✅
- `cargo build --bin mora-lsp` ✅
- `cargo test --lib` 47 passed ✅
- `mora --check typeck_errors.mora` 23 errors（类别 11 废弃）✅

### 4.2 v0.07.1: req.json() + :: 语法 + e2e 验证脚本 + Benchmark

**目标**：补齐最后 3 个遗留项 + 修复 `Router::new()` 的解析器支持。

**改动**：

- `src/lexer.rs`：`:` 后跟 `:` 切为 `TokenType::ColonColon` 新 token
- `src/ast.rs`：Expr 加 `NamespaceRef { namespace, name, span }` 变体
- `src/parser.rs`：
  - `primary()` 中 `IDENT + ColonColon + IDENT` 解析为 `NamespaceRef`
  - `call()` 中 `NamespaceRef + LParen` 解析为 `Call(qualified_name)`
  - `ColonColon` 后的 `.route()` 链式调用自动转为 `Call("Router::route")`
  - `consume_method_name()` 兼容 `Route` 关键字
- `src/interpreter.rs`：
  - `evaluate(NamespaceRef)` → 拼接 `"Router::new"` / `"McpServer::new"` 等 builtin
  - `call_method(Value::Dict, "json")` → `req.json()` 实现，解析 body 返回 `{ok: ...}` 或 `{err: "ParseError: ..."}`
- `src/typeck.rs`：`NamespaceRef` match arm `Type::Any`
- `src/lsp/providers.rs`：`NamespaceRef` match arm
- `test_data/v0.07_validation.mora`：12 项 e2e typeck 验证
- `examples/bench_server.mora`：Router::new() 显式 API benchmark 脚本

**验收**：
- `cargo build` ✅
- `cargo test --lib` 47 passed ✅
- `mora --check v0.07_validation.mora` 0 errors ✅
- `mora --check bench_server.mora` 0 errors ✅

### 4.3 v0.07.2: 更新 examples 为显式 API + 修复 consume_method_name

**目标**：删除 3 个使用旧 `serve as` 语法的 example 文件，新增 3 个使用显式 API 的替换版本；同时修复 `consume_method_name()` 对关键字的兼容性。

**改动**：

- `src/parser.rs`：
  - `consume_method_name()` 重写：从逐个关键字匹配改为通用 token→字符串映射表，覆盖所有 30+ TokenType 关键字
  - 新增 Route/ReadBytes/WriteBytes/Read/Write/Append/Let/Task/If/For/In/Import/As/Do/WithKeyword/Save/Load/Fn/Into/Stream/Tool/Break/Continue/Observe/Span/Tags/Record/Trace/Metrics/Otel/Export/Parallel 共 32 个关键字的字符串映射
  - 修复 `mcp.tool()` 等链式调用中关键字被误识别为不可解析 token 的问题
- `examples/serve_as_http_demo.mora` → **删除**
- `examples/serve_as_mcp_demo.mora` → **删除**
- `examples/serve_with_route_demo.mora` → **删除**
- `examples/http_server_demo.mora` → **新增**：多路由 + JSON body + 路径参数 + query dict + `req.json()`
- `examples/mcp_server_demo.mora` → **新增**：McpServer::new() + `.tool()` + `.serve()`
- `examples/observe_route_server_demo.mora` → **新增**：trace + span 嵌套 + AI 路由 + Router 显式 API

**验收**：
- `cargo build` ✅
- `cargo test --lib` 47 passed ✅
- `mora --check http_server_demo.mora` 0 errors ✅
- `mora --check mcp_server_demo.mora` 0 errors ✅
- `mora --check observe_route_server_demo.mora` 0 errors ✅
- 全部 6 个 examples typeck 通过 ✅

**examples 对照表**：

| 旧文件（已删除） | 新文件 | v0.07.0 typeck | v0.07.2 typeck |
|------|------|:---:|:---:|
| `serve_as_http_demo.mora` | `http_server_demo.mora` | ❌ | ✅ |
| `serve_as_mcp_demo.mora` | `mcp_server_demo.mora` | ❌ | ✅ |
| `serve_with_route_demo.mora` | `observe_route_server_demo.mora` | ❌ | ✅ |
| `observe_demo.mora` | （不变） | ✅ | ✅ |
| `route_demo.mora` | （不变） | ✅ | ✅ |
| — | `bench_server.mora`（新增） | — | ✅ |

---

## 5. 文件改动清单

| 文件 | v0.07.0 | v0.07.1 | v0.07.2 | 合计 | 说明 |
|------|---------|---------|---------|------|------|
| `src/lexer.rs` | +4 / -10 | +4 | — | +8 / -10 | 关键字清理 + ColonColon |
| `src/ast.rs` | +2 / -58 | +2 | — | +4 / -58 | Stmt::Try/Serve + 类型移除，NamespaceRef 新增 |
| `src/parser.rs` | +6 / -171 | +56 | +44 / -36 | +106 / -207 | try/serve 解析移除，:: 语法，consume_method_name 重写 |
| `src/interpreter.rs` | -220 | +38 | — | +38 / -220 | Try/Serve 分支移除，req.json() + NamespaceRef evaluate |
| `src/typeck.rs` | +10 / -63 | +4 | — | +14 / -63 | Try/Serve 检查移除，NamespaceRef typeck |
| `src/lsp/providers.rs` | +1 / -39 | +1 | — | +2 / -39 | Try/Serve walk 移除，NamespaceRef |
| `src/http_server.rs` | +1 / -3 | +1 | — | +2 / -3 | HttpMethod import 移除 |
| `src/main.rs` | +4 / -2 | — | — | +4 / -2 | banner v0.07 更新 |
| `Cargo.toml` | +1 / -1 | — | — | +1 / -1 | version 0.0.7 |
| `Cargo.lock` | +1 / -1 | — | — | +1 / -1 | 自动更新 |
| `test_data/typeck_errors.mora` | +2 / -10 | — | — | +2 / -10 | 类别 11 废弃 |
| `test_data/v0.07_validation.mora` | — | +140 | — | +140 | 新增 12 项验证 |
| `examples/observe_demo.mora` | +1 / -4 | — | — | +1 / -4 | 注释更新 |
| `examples/bench_server.mora` | — | +31 | — | +31 | Benchmark 脚本 |
| `examples/http_server_demo.mora` | — | — | +34 | +34 | 新：Router 显式 API demo |
| `examples/mcp_server_demo.mora` | — | — | +17 | +17 | 新：McpServer 显式 API demo |
| `examples/observe_route_server_demo.mora` | — | — | +29 | +29 | 新：observe + route + Router 综合 demo |
| `examples/serve_as_http_demo.mora` | — | — | 删除 | 删除 | 旧 serve as 语法废弃 |
| `examples/serve_as_mcp_demo.mora` | — | — | 删除 | 删除 | 旧 serve as 语法废弃 |
| `examples/serve_with_route_demo.mora` | — | — | 删除 | 删除 | 旧 serve as 语法废弃 |

---

## 6. 关键设计决策

| 决策 | 选择 | 理由 |
|------|------|------|
| try-catch 删除方式 | 全链路删除（lexer→ast→parser→interpreter→typeck→lsp） | 不留无用代码，静态检查强制走 Result<?> |
| serve as 删除方式 | 全链路删除 + ServeProtocol/RouteDecl/HttpMethod 类型删除 | 显式 API 已是唯一路径 |
| `req.json()` 签名 | `dict.json() → Result<Value, ParseError>` | 与 `?` 操作符兼容，空 body/非 JSON → err |
| `::` 语法 | 新增 `TokenType::ColonColon` + `Expr::NamespaceRef` | 不再需要 hack builtin 注册表 |
| `consume_method_name()` | 从逐个匹配改为 32 个关键字的通用映射表 | 鲁棒性好，新增关键字自动可用，修复 `mcp.tool()` 等链式调用 |
| Benchmark 脚本 | 写为 `.mora` 脚本配合外部工具（ab/wrk） | 压力测试由专业工具负责，Mora 负责服务逻辑 |
| Example 命名 | 语义化命名（`http_server` / `mcp_server` / `observe_route_server`） | 比旧 `serve_as_*` 命名更直观反映内容 |

---

## 7. 当前状态（v0.07.2 完成态）

### 7.1 Type 系统全貌

```
String, Number, Bool, Nil, List, Dict, Task, Closure,
Conversation, Stream, Builtin,
AiConfig, AiResult, AiError, AiModule,
Result_(T, E), Router, HttpRequest, HttpResponse, McpServer,
Any
```

（共 20 种，比 v0.06 少了 `RouteDecl`/`ServeProtocol`/`HttpMethod` 等中间类型，它们已从 AST 中移除）

### 7.2 Value 系统全貌

```
String, Number, Bool, Nil, List, Dict, Task, Closure,
Builtin, Conversation, Stream, Agent,
AiConfig, Router, HttpRequest, McpServer
```

（共 16 种，不变）

### 7.3 测试覆盖

- typeck 单元测试：19 个，全部通过
- interpreter 单元测试：22 个，全部通过
- lsp 单元测试：6 个，全部通过
- **合计**：47 个，全部通过，0 失败，0 回归
- e2e typeck 错误：23 个命中（类别 11 try-catch 废弃）
- v0.07 validation：12 项 typeck 验证 0 错误
- examples typeck：6/6 全部通过

---

## 8. 验证矩阵

| 验证项 | v0.07.0 | v0.07.1 | v0.07.2 |
|--------|:---:|:---:|:---:|
| `cargo build --bin mora` | ✅ | ✅ | ✅ |
| `cargo build --bin mora-lsp` | ✅ | ✅ | ✅ |
| `cargo test --lib` | ✅ 47 | ✅ 47 | ✅ 47 |
| `mora --check typeck_errors.mora` | ✅ 23 errors | ✅ 23 errors | ✅ 23 errors |
| `mora --check v0.07_validation.mora` | — | ✅ 0 errors | ✅ 0 errors |
| `mora --check observe_demo.mora` | ✅ | ✅ | ✅ |
| `mora --check route_demo.mora` | ✅ | ✅ | ✅ |
| `mora --check bench_server.mora` | — | ✅ 0 errors | ✅ 0 errors |
| `mora --check http_server_demo.mora` | — | — | ✅ 0 errors |
| `mora --check mcp_server_demo.mora` | — | — | ✅ 0 errors |
| `mora --check observe_route_server_demo.mora` | — | — | ✅ 0 errors |
| `git push` | ✅ | ✅ | ✅ |

---

## 9. v0.07 → v0.08 衔接

按规划书第 4 章，v0.08 "通用现代语言"（原 v0.07 计划，因 v0.07 改为清理版重新编号）：

| 周 | 任务 | v0.07 提供的基础 |
|----|------|-----------------|
| 1-4 | trait + interface | Type 枚举框架 |
| 5-8 | 泛型 | Type::Result_(T,E) 已有泛型原型 |
| 9-12 | 标准库（HTTP client / 时间 / 文件 / 加密 / SQL） | http_server 已成熟，file.* 已完整 |
| 13-16 | AI 长期记忆标准库 | AiConfig/AiError/AiResult 类型已就位 |
| 17-20 | Agent 升级 | Value::Agent 已存在 |
| 21-24 | 包管理 + semver | Cargo.toml 已有版本号 |
| 25-28 | 测试框架 | — |
| 29-32 | 并发（async fn/await） | — |

---

**创建日期**：2026-06-18
**作者**：Mora 核心团队
**License**：BSD-3-Clause
