-- v0.09: 泛型 Container trait demo（语法解析测试）
-- 完整语义验证在 trait_tests 模块里

trait Container<T>
  fn get(self) -> T
end

impl Container<number> for BoxedNum
  fn get(self) = 42
end

task main()
  let c: dyn Container<number> = Container<number>::new("BoxedNum")
  print(c.get())
end