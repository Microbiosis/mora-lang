export let default_model = "gpt-4"

export task summarize(input)
  prompt = "Summarize: {input}"
  return ai.chat(prompt)
end

export task classify(input)
  prompt = "Classify: {input}"
  return ai.chat(prompt)
end
