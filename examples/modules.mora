import "examples/lib/tools"

task main()
  let text = web.fetch("https://example.com")
  let summary = summarize(text)
  print(summary)

  let category = classify("Hello world")
  print(category)

  print("Model: " + default_model)
end
