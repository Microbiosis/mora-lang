app Counter
  model: Counter
  msg: CounterMsg
  init: {count: 0, step: 1}
  update: fn(m, msg) => m
  view: fn(m) => m
end

let app_inst = tea.init({count: 0, step: 1})
tea.dispatch(app_inst, {tag: "Increment"})
tea.dispatch(app_inst, {tag: "Increment"})
tea.run(app_inst, 5)
tea.model(app_inst)
